# Tasks: S5 — Protected Learning

**Feature**: [spec.md](spec.md) | **Plan**: [plan.md](plan.md) | **Data model**: [data-model.md](data-model.md) | **Contracts**: [contracts/learning-api.md](contracts/learning-api.md) | **Quickstart**: [quickstart.md](quickstart.md)

**Date**: 2026-07-29 | **Review Tier 3.** **A builder never closes its own slice.**

**S2, S3, and S4 are closed** on independent verdicts. S5 is active and unblocked, but implementation
has not started: all tasks remain uncompleted and these approved planning artifacts stay frozen. S5
consumes S4's evaluator, signed issuance, trusted duration, and non-production seed; S2's Course
graph; S3's bilingual shell.

---

## Standing clause

> **A required dependency is validated at construction and the component refuses to build without it.
> No security-relevant control may silently degrade, default, or become optional.**

In S5 this clause forbids three constructions specifically:

- a handler that authorises from a **decision cached** at page load, session start, or a prior request;
- a Progress path that **creates an Enrollment** to make itself work;
- a denial that is **distinguishable** from non-existence.

Each would read as reasonable and each would ship a second access model.

**Tests are required, and every acceptance proof must fail under a deliberate mutation** (SC-012).
Eight instances of one defect class — *a control that reads as enforcement and enforces nothing* —
have appeared across S1C and S2. In every case the builder's own report said the work was clean.

## The boundary every task in this file respects

> **S5 owns physical schema introduction required by protected learning. S6 owns enrollment lifecycle
> semantics and production mutations.**

No task below implements invitation, approval, grant, rejection, revocation, or account-access
workflow. No task below creates an Enrollment **row** in a production path. No task below implements
a community link — FR-036 – FR-038 are `DEFERRED — S18` under
[D-046](../../docs/DECISIONS.md#d-046--the-external-course-community-link-is-deferred-to-post-launch).

---

## Phase 1 — Setup

- [X] T001 Derived from the pre-S5 `0012_media_and_entitlement` maximum: S5 takes `0013` and `0014`; `db.MaxSchemaVersion` follows the resulting `0014` value ([research.md R-02](research.md#r-02--migration-numbering-and-the-s5s6-split)).
- [X] T002 [P] Create `backend/internal/learning/doc.go` stating the boundary: consumes access decisions, creates none. **No `Enroll`, no `Grant`, no `Create`, no exported constructor for an Enrollment** (FR-005, FR-015a)
- [X] T003 [P] Add `hls.js` to `frontend/package.json` and create `frontend/src/components/learning/` ([research.md R-07](research.md#r-07--player-hlsjs-with-native-fallback))

## Phase 2 — Foundational (blocking prerequisites for every user story)

**No user story can start until this phase completes.** The schema and the access boundary are what
every story is built on.

- [X] T004 Add migration `0013_enrollments.{up,down}.sql` in `backend/internal/db/migrations/` creating `enrollments` with **exactly** `id`, `student_account_id` (FK → `accounts`), `course_id` (FK → `courses`), `created_at`, and `UNIQUE (student_account_id, course_id)` — the shape [S6 asserts](../006-course-access-grant/data-model.md) verbatim. **No `status`, `enrolled_via`, `entitlement_id`, `revoked_at`, or any lifecycle column** — each encodes a judgement S6 owns (BR-114, BR-167, Principle IV)
- [X] T005 Add migration `0014_protected_learning.up.sql` step 1: **assert the legacy `progress` table is empty and `RAISE EXCEPTION` naming the row count if it is not**. Do **not** drop rows and do **not** synthesise an Enrollment to preserve them — that fabricates provenance for a grant that never happened (FR-018, FR-015a, Principle VII, [research.md R-01](research.md#r-01--the-legacy-progress-cutover-cannot-preserve-rows-and-must-not-synthesise-enrollments))
- [X] T006 Add `0014_protected_learning.up.sql` step 2: drop the legacy `progress` table and create it at the BR-116 identity per [data-model.md](data-model.md) — `enrollment_id UUID NOT NULL` FK → `enrollments(id)` (**never nullable, never re-keyed later**), `course_lesson_identity_id UUID NOT NULL` FK → `course_lesson_identities(id)`, `max_position_seconds`, `last_position_seconds`, `completed_at`, `completing_asset_version_id`, `last_watched_at`, `updated_at`, with `UNIQUE (enrollment_id, course_lesson_identity_id)`, the four `CHECK` constraints, and `idx_progress_enrollment`. The durable key is never `lessons(id)` or a revision row *(FR-015, BR-114, BR-116, D-060)*
- [X] T007 Add `content_reports` to `0014_protected_learning.up.sql` per [data-model.md](data-model.md), including the closed `target_kind` and `reason` `CHECK` enumerations — **the fixed reason set exactly as specified; introduce no new reason** — plus `rep_other_needs_explanation` and the partial unique index `rep_no_duplicate_open` *(FR-029, FR-032, BR-145)*
- [X] T008 Write `0013` and `0014` down migrations. `0014`'s down must run before `0013`'s — the Progress foreign key depends on `enrollments`
- [X] T009 Raise `db.MaxSchemaVersion` to the value T001 derived (expected **14**) and confirm CI **derives** its assertion from that constant rather than hardcoding a literal — the convention `specs/004-public-catalogue/tasks.md` T035 sets
- [X] T010 Integration test in `backend/internal/db/` — clean install (`0001`→`0014`), upgrade path (`0010`→`0014`), and the `up`/`down`/`up` round trip, all against **real PostgreSQL**. Assert `0013` applies before `0014` on both paths
- [X] T011 [P] Integration test `TestEnrollmentsShapeMatchesS6Contract` in `backend/internal/db/` asserting the `enrollments` columns, types, nullability, and unique constraint match [S6's asserted contract](../006-course-access-grant/data-model.md) exactly. This is the cross-slice contract; a divergence found in August costs a rebase of two slices
- [X] T012 [P] Integration test `TestLegacyProgressGuardRefusesNonEmptyTable` in `backend/internal/db/` — insert a legacy row, run the migration, assert it **aborts** naming the row count and preserves the row
- [X] T013 Create `backend/internal/learning/repo.go` reading `enrollments` to resolve an `enrollment_id`. **It exposes no insert, update, or delete against that table** (FR-015a)
- [X] T014 Create `backend/internal/httpapi/learning_foundation.go` with `NewLearningFoundation` and the `WithLearningFoundation` `RouterOption`, mirroring `catalog_foundation.go`. **Required dependencies validated at construction** — a foundation constructible without its entitlement evaluator is the standing clause's forbidden construction
- [X] T015 Implement the **single uniform refusal constructor** in `backend/internal/httpapi/learning_handlers.go`. Every non-allow outcome — expired, revoked, out-of-scope, suspended, emergency-suspended, retired-ineligible, and target-not-found — maps through it to one byte-identical `404 application/problem+json`. **No handler writes its own refusal** (FR-003, BR-023, BR-050, [research.md R-05](research.md#r-05--denial-uniformity-and-the-one-place-it-is-decided))
- [X] T016 Log and audit the **typed** denial reason internally while the external response stays uniform (Principle IX). The reason must not reach the body, status, header set, or timing
- [X] T017 Add the Student-Account-only guard to every S5 route: an Instructor or Admin Account receives the **uniform refusal**, not a role-specific error (FR-007, BR-082)
- [X] T018 [P] Unit test in `backend/internal/learning/` asserting no exported symbol in the package can create an Entitlement or an Enrollment row
- [X] T019 Build-constraint test `TestProductionBuildHasNoEnrollmentCreationPath` asserting the **production build** contains no Entitlement-creating and no Enrollment-row-creating symbol, in the same shape as S4's seed-exclusion assertion (FR-005, FR-015a, SC-006)

**Checkpoint A — MANDATORY GATE. Blocks every user story.** T019 passes and T011 proves the shape.
No Progress work begins before creation is proven **impossible**, not merely absent.

**Checkpoint C — denial uniformity.** T015 and T016 land together. A typed reason that reaches the
response is the content inventory this slice exists to prevent.

---

## Phase 3 — User Story 1 (P1): a Student watches a Lesson and never loses their place

**Goal**: resume position and per-Lesson completion, server-authoritative, non-regressing.

**Independent test**: seed an active Entitlement via S4's non-production seed, insert an Enrollment
fixture directly, play to a position, terminate the session, re-open, assert resume position and
completion state **server-side**.

- [X] T020 [P] [US1] Implement position bounding in `backend/internal/learning/progress.go` — clamp into `[0, trusted duration]`. A position beyond the duration or below zero is **clamped, not rejected**, so a bad tick does not lose the session (FR-011)
- [X] T021 [P] [US1] Implement server-side completion in `backend/internal/learning/completion.go` — at least **90% of the trusted duration of the exact Media Asset Version played**. Any client-reported percentage or duration is **ignored**, not validated as a hint (FR-010, BR-051)
- [X] T022 [US1] Implement the monotonic upsert in `backend/internal/learning/progress.go` using `ON CONFLICT (enrollment_id, course_lesson_identity_id) DO UPDATE` with `GREATEST` for the maximum and `COALESCE` for `completed_at` and `completing_asset_version_id`. Monotonicity and write-once completion are **database semantics**, not application checks. `last_position_seconds` is deliberately **not** monotonic — it is the resume point. The upsert's conflict target is the stable BR-116 Progress identity (FR-012, FR-015, BR-114, BR-116, D-060, [data-model.md](data-model.md#the-monotonic-upsert))
- [X] T023 [US1] Implement `PUT /api/v1/learn/lessons/{lessonId}/progress` in `backend/internal/httpapi/learning_handlers.go` in the contract's order: **revalidate access at request time**, resolve the Enrollment (read only), bound, compute completion, upsert (FR-014, [contracts/learning-api.md](contracts/learning-api.md#put-apiv1learnlessonslessonidprogress))
- [X] T024 [US1] Add the Progress-write rate limit — **12 writes / min / (Student, Lesson)** — using `backend/internal/ratelimit`, sized against the 15 s reporting interval per FR-017 ([research.md R-04](research.md#r-04--progress-reporting-interval-and-rate-limit-sizing))
- [X] T025 [US1] Implement `POST /api/v1/learn/lessons/{lessonId}/playback` requesting a freshly issued, short-lived, **session-scoped** signed URL from S4 per playback session. **Never cached, persisted, shared, or reused** across sessions or Students (FR-008, BR-100) **Amended 2026-08-06 (review remediation).** T025 also owns the **playback** half of FR-017 and BR-102, which previously had no owning task: playback issuance enforces **30 issuances / 10 min / Student** keyed on the Student alone, plus a separate **600 issuances / 10 min** source-address ceiling ([D-071](../../docs/DECISIONS.md#d-071--playback-issuance-enforces-two-rate-ceilings-and-the-gate-opens-exactly-one-production-exception)). Both are decided before authorization and before any issuance, source address first then Student; both fail closed; a quota denial is `429` with `Cache-Control: no-store` and a valid `Retry-After`. **This scope was absent from the implementation commit `5cc8ede0` and was added later in `6d14657d`** after independent Tier 3 review raised it as H-1 against head `f5985c7`. The amendment records where the control now lives; it does not claim the original implementation contained it. Executable evidence: `backend/internal/httpapi/learning_playback_rate_limit_test.go` — 7 tests covering decision order before authorization, source-ceiling denial, Student-ceiling denial after the source allows, fail-closed on an undecidable ceiling for each ceiling independently, key independence, and the authoritative quota values.
- [X] T026 [US1] Add the Progress source-address ceiling — **1,200 requests / min** with a **120-request burst**, keyed by trusted normalized source address (individual IPv4, IPv6 `/64`), before the existing 12 writes / min / `(Student, stable Lesson)` limit and before any Enrollment, media, or Progress access. The source limiter fails closed; a quota denial is `429`, `Cache-Control: no-store`, and valid `Retry-After` (FR-017, BR-102, D-061)
- [X] T027 [P] [US1] Build the Lesson Player at `frontend/src/app/[locale]/learn/courses/[courseId]/lessons/[lessonId]/page.tsx` with `hls.js` and native HLS fallback. **Server-rendered per request, no caching** of authorisation-bearing payloads
- [X] T028 [P] [US1] Implement the client progress reporter in `frontend/src/components/learning/` — every **15 s**, plus pause, seek-settled, `visibilitychange`→hidden, and `pagehide` via `sendBeacon`. Use `pagehide`, **not** `unload`, which is unreliable on mobile Safari ([research.md R-04](research.md#r-04--progress-reporting-interval-and-rate-limit-sizing))
- [X] T029 [US1] Implement D-062's bounded client retry-with-backoff — three total attempts; 2 s then 4 s exponential ±20% jitter; only network, 408, 429, 500, 502, 503, and 504 retry; 429 honors valid `Retry-After` or falls back to 15 s — so a **transient Progress-write failure never interrupts** an otherwise-authorised playback session. Use one Lesson/Asset-Version-scoped single-flight controller with coalescing and lifecycle disposal, and replace T028's incompatible `sendBeacon` branch with same-origin keepalive JSON `PUT` fetch (FR-013, FR-014, SC-008, D-062).
- [X] T030 [P] [US1] Unit tests in `backend/internal/learning/` for bounding and the ≥90% completion calculation, including falsified client percentages and durations (FR-010, FR-011, SC-004)
- [X] T031 [US1] Integration tests against **real PostgreSQL** in `backend/internal/learning/`: resume after session termination; completion at ≥90% writing `completed_at` **once**; no regression across seek-backwards, replay, retry, reconnection, and **Instructor video replacement** (FR-009, FR-012, BR-059, SC-003, SC-007)
- [X] T032 [US1] **Concurrency test** `TestProgressConcurrentWritersPreserveMonotonicMaximum` — N concurrent writers at one `(enrollment, lesson)` with interleaved, out-of-order positions; assert the final maximum is the true maximum and `completed_at` was written **exactly once**. Run under `-race`. This is Constitution **Principle V**'s requirement discharged on S5's structural equivalent of a grant path ([research.md R-06](research.md#r-06--monotonicity-under-concurrency))
- [X] T033 [US1] Security integration test: a **delayed, retried, duplicated, or out-of-order** Progress write arriving **after** access ended is **refused**. The happy path never exercises this (FR-014, BR-053)

**Checkpoint D — monotonicity under concurrency.** T032 passes. Sequential tests cannot see the
two-device race, and it is the one that loses a Student's progress in production.

---

## Phase 4 — User Story 2 (P1): access that has ended stops working immediately, everywhere

**Goal**: every denial cause is effective for an **already-open** session, and every denial is
indistinguishable from non-existence.

**Independent test**: start an authorised playback session, mutate the access condition server-side
mid-session, assert the next issuance, next segment authorisation, and next Progress write are all
denied **without the client cooperating**.

- [X] T034 [US2] Wire every protected S5 handler to call `entitlement.Evaluate(student, lesson, now)` **in its own handler path, at request time** — no memoisation, no request-scoped cache, no session-scoped cache. **No handler compares an expiry, checks a revocation flag, or inspects scope itself** (FR-001, FR-005, Principle IV)
- [X] T035 [US2] Ensure no S5 payload carries a token, capability, or decision the client could re-present. Payloads carry **rendering state only** ([plan.md](plan.md#the-cached-decision-failure-and-how-the-code-prevents-it))
- [X] T036 [US2] Confirm the BR-027 retirement path: a qualifying Entitlement against a **delisted, retired, or archived** Course continues to authorise, subject to `retirement_eligibility_at` vs `retired_at`. This is delegated to S4's evaluator — assert S5 does not add its own comparison (FR-004, BR-050, BR-090)
- [X] T037 [US2] Security integration test `TestDenialsAreByteIdentical` in `backend/internal/httpapi/` — all seven causes (expired, revoked, out-of-scope, suspended, emergency-suspended, retired-ineligible, **never-authored Lesson id**) return **byte-identical** responses. Compare the **full** response; asserting only the status code passes while leaking existence through the body (FR-003)
- [X] T038 [US2] Production-router test `TestEveryProtectedLearningRouteRevalidates` in `backend/internal/httpapi/` — enumerate protected S5 routes from the **mounted production router** via `r.Routes()`, **not** a hand-maintained list, and prove each denies after a mid-session access mutation. A hand-maintained matrix is the S1C finding; a sweep that tests its own router is the S2 finding (FR-001, SC-002)
- [X] T039 [US2] Extend `TestAuthorizationMatrixMatchesMountedRouter` in `backend/internal/httpapi/authorization_test.go` to cover every S5 route
- [X] T040 [US2] Integration test: on **every** denial cause, **no** Entitlement, Enrollment, or Progress record is mutated as a side effect (FR-016, BR-026)
- [X] T041 [US2] Integration test: a signed URL issued moments before access ended does **not** extend access beyond S4's token boundary, and **no new issuance** is granted (FR-002, BR-100)
- [X] T042 [US2] End-to-end test `frontend/e2e/s5-access-ends.spec.ts` mutating each condition — expiry, revocation, Account suspension, emergency Course access suspension — **mid-session**, asserting the next issuance and next Progress write are denied (SC-005)
- [X] T043 [US2] End-to-end test: an **expired** Entitlement shows retained Enrollment, retained Progress, and an expired state, while **nothing** is authorised from any of it. Progress is history, never an authorisation input (FR-016, BR-029)
  Evidence: `frontend/e2e/s5-expired-entitlement.spec.ts` drives the real Next.js routes, the production Go API, production authentication and session middleware, S4 entitlement evaluation, and the D-063 read models against an isolated per-run PostgreSQL database with production migrations and no protected-route interception. The retained-expired fixture keeps the Enrollment, one partial and one completed Progress row, a READY video Asset Version, an authored Resource, and an authored Lab Material beneath an Entitlement that expired at a deterministic UTC instant. Dashboard, Course Home, and Lesson retain the authored graph, aggregate and per-Lesson Progress, completion state, and navigation in Arabic RTL and English LTR at phone and desktop, with an explicit localized expired state and the original RFC 3339 instant preserved identically in `<time datetime>` across both locales. `POST /playback`, `PUT /progress`, and both D-064 material entry routes each return the uniform `404` `application/problem+json` refusal with `Cache-Control: no-store`, no `Location`, no signed target, and no internal reason, while test-runner-side PostgreSQL snapshots prove the Entitlement, Enrollment, and Progress are byte-identical before and after every attempt.

**Checkpoint B — request-time revalidation. Blocks slice closure.** T038 passes **and** the SC-002
mutation holds: delete one revalidation call and a test must go red. A revalidation whose removal
breaks nothing was never load-bearing.

---

## Phase 5 — User Story 3 (P2): a Student navigates a Course and sees exactly their scope

**Goal**: Course Home, the Dashboard learning half, and the player shell — bilingual, responsive,
accessible.

**Independent test**: render Course Home for an entitled Student against a seeded multi-Section
Course; assert ordering, progress aggregation, expiry display, and RTL/LTR at representative
viewports.

- [X] T044 [US3] Implement the qualifying Course-graph read in `backend/internal/learning/coursehome.go` — Sections and Lessons in **authored order** from the current approved or qualifying acquired graph (FR-019, BR-010, BR-017, BR-027)
- [X] T045 [US3] Implement per-Course progress aggregation — completed Lessons over total Lessons in the qualifying graph. **No duration weighting, no partial credit** (spec §Assumptions)
- [X] T046 [US3] Implement `GET /api/v1/learn/courses/{courseId}` per [contracts/learning-api.md](contracts/learning-api.md#get-apiv1learncoursescourseid) and D-063's direct read-model schema. Every Section and Lesson of an entitled Course is in scope — Course scope is whole-Course (FR-020, BR-021, BR-024). **No community-link field** (D-046). Production-router PostgreSQL evidence covers authored graph, stable Progress, current revision, retained expiry, isolation, no side effects, and fail-closed incomplete graphs.
- [X] T047 [US3] Implement `GET /api/v1/learn/courses/{courseId}/lessons/{lessonId}` per D-063, returning Lesson metadata, outline navigation, previous/next stable targets, resume position, and completion state. **Carries no signed URL and no token** (FR-024). PostgreSQL contract evidence covers cross-Section navigation, stable binding, stale identifiers, and exact field exclusion.
- [X] T048 [US3] Implement `GET /api/v1/learn/dashboard` per D-063 — Continue Learning and My Courses with deterministic Enrollment/Course ordering, per-Course progress, and active/retained-expired state. **Reads no Course Access Invitation state**; ST10 Access History and the invitation panel are S6's (FR-023, FR-006, BR-029). Bulk classification and Student-isolation evidence cover omission and exact empty results.
- [X] T049 [US3] Implement D-063's five distinct rendering states — active, expired, delisted-but-accessible, emergency-suspended, content-unavailable — per [plan.md](plan.md#fail-closed-rendering-states). A Lesson whose video is missing, quarantined, scan-failed, or transcode-failed is **reachable and marked unavailable** while the rest of the Course stays usable (FR-022, BR-090). Backend evidence keeps the read model reachable without media authority, preserves qualifying delisted access, denies emergency suspension, and retains expired display while playback/Progress remain denied.
- [X] T050 [P] [US3] Build Course Home at `frontend/src/app/[locale]/learn/courses/[courseId]/page.tsx`, server-rendered per request with **no caching** of authorisation-bearing payloads. D-063 server adapter and dynamic page evidence use the Course Home read model, authored graph order, stable Progress, and generic unavailable handling.
- [X] T051 [P] [US3] Build the Dashboard learning half at `frontend/src/app/[locale]/learn/page.tsx`. The approved implementation uses the localized `/learn/dashboard` surface, the same D-063 Dashboard contract, server ordering, active/expired presentation, empty state, and no stale fallback.
- [X] T052 [P] [US3] Build the outline rail and previous/next navigation in `frontend/src/components/learning/`. Lesson read-model integration preserves server navigation IDs, null boundaries, saved position, expired/no-player behavior, and the existing separate playback/Progress path.
- [X] T053 [US3] Implement **custom platform-owned player controls** — play, pause, seek, volume, quality selection, fullscreen where available. Custom rather than native because native control sets are inconsistently labelled across browsers and cannot be audited by the automated WCAG check SC-009 requires (FR-024, [research.md R-07](research.md#r-07--player-hlsjs-with-native-fallback))
  Evidence: `PlayerControls` replaces native controls while preserving the existing S4 playback, hls.js/native HLS, Asset-Version-scoped reporter, and cleanup paths. Deterministic media/HLS/fullscreen behavior tests pass.
- [X] T054 [US3] Make every platform-owned control keyboard-operable and screen-reader-labelled to **WCAG 2.2 AA**. **Do not claim complete product-level conformance** while captions are outside MVP (FR-025, `LG-015`)
  Evidence: native buttons, range inputs, and select semantics provide keyboard operation; localized state-dependent names, values, focus styles, unsupported-control omission, and generic failure behavior are covered by the deterministic control tests. Full browser axe certification remains T061.
- [X] T055 [US3] Render every S5 screen in Arabic and English with correct RTL/LTR, **Arabic default**, persistent preference, via the existing `[locale]` segment and `frontend/src/lib/i18n`. **Instructor-authored content is never translated** (FR-026, BR-149, BR-150)
  Evidence: learning pages use the existing dictionaries and `LocaleProvider`/`gradex.locale` persistence; the protected `[locale]/learn` segment now remains authoritative for direction and content, and the route-preserving learning language toggle uses the same storage mechanism. Deterministic guards cover Arabic default, RTL/LTR, matching dictionary keys, authored-title preservation, and equal generic denial presentation. Full viewport and browser E2E evidence remains T060/T061.
- [X] T056 [US3] Display per-Lesson and per-Course progress and the **access-until instant**, rendered in the Student's locale (FR-021, BR-025, BR-052)
  Evidence: pure learning formatters render backend completed/total/percent and saved position with locale-aware numerals; expiry uses the documented `Asia/Kuwait` fallback when no Student timezone is known, preserves the original RFC 3339 value in a semantic `<time dateTime>`, and renders the null-expiry contract without inventing a date. Deterministic formatter and source guards cover active/expired values, zero/partial progress, Arabic/English output, and no client-side authorization inference.
- [X] T057 [US3] Link to **S4's** protected Resource and Lab Material download entry points without issuing, signing, proxying, or caching those links. ST08 is S4's screen (FR-028, BR-103, BR-143). D-064 adds fixed same-origin S4 GET entry routes with fresh authorization/signing and uniform no-store refusal; D-063 read models expose only ordered `materials` kinds, while active Course Home/Lesson surfaces render ordinary non-prefetched anchors and expired/unavailable states render none. Focused router, real-PostgreSQL delivery, bulk discovery, contract, and frontend route tests pass.
- [X] T058 [US3] Integration tests in `backend/internal/learning/`: authored ordering, whole-Course scope, per-Course aggregation, and the five rendering states
- [X] T059 [US3] Integration test: a Student holding Entitlements for two Courses sharing an Instructor sees **no cross-Course leakage** in either Course Home (spec §Edge Cases)
- [X] T060 [P] [US3] End-to-end test `frontend/e2e/s5-course-home.spec.ts` — **Arabic RTL and English LTR** at phone, tablet, laptop, and desktop, with **retained rendered evidence** (the S2 T066 standard). Assert no Student capability is missing at any viewport (FR-027, SC-010)
- [X] T061 [P] [US3] End-to-end test `frontend/e2e/s5-lesson-player.spec.ts` — same four viewports, both directions, plus **keyboard-only operation** and an **automated WCAG 2.2 AA scan with zero violations** for platform-owned controls (SC-009, SC-010)

---

## Phase 6 — User Story 4 (P3): a Student reports content

**Goal**: reports reach the Admin queue; nothing about the reported content changes.

**Independent test**: submit reports from an entitled and a non-entitled Student against each target
type; assert queue entry, rate limiting, duplicate refusal, and that the reported content stays
visible.

- [X] T062 [US4] Implement report creation in `backend/internal/learning/report.go` recording **both** the stable logical target and the **exact visible** Course revision or Media Asset Version at submission (FR-030, BR-145)
- [X] T063 [US4] Implement `POST /api/v1/learn/reports`, refusing a report from a Student holding **no Entitlement** for the target's Course via the uniform refusal (FR-033)
  Evidence: the route is mounted on the production learning group behind the same authentication and Student-capability gate as every other protected route. The body names no target — only the opaque encrypted context, the reason, and an optional explanation — under the established 16 KiB protected-learning mutation bound and the repository's strict JSON admission. Context verification (envelope, version, purpose, expiry, future-issuance, reporter Account, authenticated session) precedes a current S4 Entitlement decision that runs **inside the insert's own transaction** ([R-14](research.md#r-14--report-submission-is-authorised-inside-its-own-insert-transaction)), so access ending after issuance refuses the write. Through the real router against real PostgreSQL, all five kinds — COURSE, LESSON, VIDEO, RESOURCE, LAB_MATERIAL — file from a page rendered at A after B goes live and store **A**; a fresh read then issues B, and after resolving the open report as fixture setup, B stores B while the original stays bound to A. Nineteen refusal causes across cryptography, current authority, and relational validity produce one byte-identical `404`, write no row, and mutate no authority; duplicates return `409` naming nothing about the earlier report; concurrent submissions produce exactly one row with the database as the authority. The 5/hour throttle remains T064's and is not implemented here.
- [X] T064 [US4] Add the report rate limit — 5/hour/Student — via `backend/internal/ratelimit`. This is **separate from** the duplicate constraint in T007; they fail differently and FR-032 requires both ([research.md R-11](research.md#r-11--reports-are-rate-limited-and-deduplicated-which-are-different-controls))
  Evidence: `learning-report-v1` is a fixed one-hour window of five, one dimension — the authenticated Account — with no burst and no source dimension, registered as a required learning policy so the foundation refuses to construct without a valid one. The decision runs first in the route, after authentication and the Student gate but before the body is read and before the context is decrypted, so anonymous, Instructor, Admin, and suspended callers spend nothing while **every** authenticated submission costs one attempt whatever it would have returned — proven at the route for `201`, `409`, `400`, `422`, and the protected `404` ([R-15](research.md#r-15--the-report-submission-throttle)). Redis and the bounded local fallback carry the identical limit and key: both admit exactly five and refuse the sixth through the real router, neither overshoots under concurrency, the window resets deterministically on an injected clock, and refusals never extend the TTL. Quota is Account-scoped — proven to survive a second session and to aggregate across two Courses, while a second Student is unaffected. Over quota is `429` with `Cache-Control: no-store` and an integer `Retry-After` inside the window, carrying no Course, Lesson, report, Entitlement, context, quota, limiter key, or backend detail; a limiter that cannot decide falls back to the uniform protected `404` per [D-061](../../docs/DECISIONS.md#d-061--s5-progress-source-address-ceiling). A throttled request verifies no context, evaluates no Entitlement, reaches no repository, and mutates nothing; T063's exact-visible A→B binding still stores A for all five kinds with the throttle in place, and duplicate refusal remains the database's.
- [X] T065 [US4] Ensure the acknowledgement reveals **nothing** about Admin queue state, other reports, queue position, or moderation outcomes (FR-034, BR-146)
  Evidence: the `201` body is a two-property allowlist — `report_id` and `created_at` — built from the stored report **by name** rather than by serializing it, so a column added to `content_reports` arrives at the constructor unread ([R-16](research.md#r-16--the-acknowledgement-is-an-allowlist-not-a-projection)). A reflection guard fills every domain field with a sentinel and proves none reaches the wire; a second guard proves the response struct has no embedded, map, interface, or untagged field through which one could. No `status` or moderation field, no queue position or priority, no similar-report count, no `Location`, `ETag`, or `Last-Modified`, and `Cache-Control: no-store`. Against real PostgreSQL through the production router: the two published values correlate to exactly one persisted row while the reporter, stable target, exact version reference, reason, and explanation stay server-side; all five target kinds return an identical schema and header set, so the response reveals neither which kinds carry an Asset Version nor whether a stale context bound Revision A rather than B — while the database still asserts A stores A and B stores B. Two Students reporting the same target learn nothing of each other's report, identifier, timing, or the total. The duplicate `409`, protected `404`, and throttle `429` are the shared problem envelope and nothing more, carrying no acknowledgement field, no collided-report data, and no remaining quota; field-level `422` violations name the offending request field and echo neither the encrypted context nor the explanation, in responses or logs. Concurrent submissions each acknowledge only their own row, with no shared identifier and no queue order.
- [X] T066 [P] [US4] Build the Report Content modal in `frontend/src/components/learning/`, reachable from Course Home (Course target) and the Lesson Player and material lists (Lesson, video, Resource, Lab targets), in both locales and directions
  Evidence: `report-content-dialog.tsx` is a Radix dialog — semantic title, described content, focus trap and return, named close control, Escape and outside-dismiss suppressed only while a submission is in flight, `dir` per locale, 44px targets, and a viewport-bounded scrolling panel. Actions are derived in `report-targets.ts` from the issued context alone: Course Home offers the COURSE action only when its active read carried `report_context`, a Lesson offers exactly the kinds present in `report_contexts`, and retained-expired, unavailable, and Dashboard reads offer none — availability is never inferred from a title, a mounted player, or a material link. Reasons come from the server's closed set and are shown only as localized copy in both languages, `other` requires a non-blank explanation before the request as the database constraint does after it, and one in-flight submission is enforced per dialog with no automatic retry anywhere. The opaque context travels from the read model to the request body unchanged and reaches nothing else — proven by contract tests asserting it is absent from URL and headers, and by structural guards limiting its consumers to three modules and forbidding storage, decoding, logging, attribute, and markup sinks. Every outcome collapses to one generic localized sentence: `201` acknowledges receipt only, `409`, `429`, `404`, and validation each say what the Student can do and never why, and no copy names a cause, a queue, a count, a policy, or another report. Scope identity closes and resets a dialog when the Course, Lesson, or kind changes and discards a late response for a target no longer displayed. A first implementation passed the whole learning dictionary into the client component and put "Active access" into the retained-expired Lesson payload; the props were narrowed to report copy and the component is no longer mounted when nothing is reportable, with a regression test pinning both.
- [X] T067 [US4] Integration tests in `backend/internal/learning/`: each target kind; every reason in the fixed set accepted and any reason outside it refused; `other` without explanation refused **at the database constraint**; duplicate refused by the partial unique index; throttling; non-entitled refusal; acknowledgement disclosure *(FR-029, FR-032, FR-033, FR-034, BR-145)*
  Evidence: `t067_report_acceptance_integration_test.go` runs against real PostgreSQL and the production migrations on a per-run isolated database. All five target kinds are accepted and store both identities — the stable logical target and the exact visible Revision or Asset Version — and all five reasons are accepted and stored as their exact wire value, each on its own target so D-066 never masks an acceptance. A reason and a target kind outside the closed sets are refused twice: by the domain, and by `rep_reason`/`rep_target_kind` reached through direct SQL the Go guard never sees. That indirection is the point for the two required mutations: `other` without an explanation is asserted against `rep_other_needs_explanation` and duplicates against `rep_no_duplicate_open` **at the database**, so dropping either turns this file red — verified by applying both mutations, observing the failures, and restoring the migration byte-identically. FR-033 is proven with the real S4 evaluator inside the report's own transaction: no Entitlement, expired, revoked, suspended Account, and emergency Course suspension each refuse and write nothing, against a positive control that succeeds and creates no Progress. The throttle and the duplicate rule are shown to be independent controls (R-11) — the production `learning-report-v1` policy refuses a sixth distinct target while the domain would accept it, and the database refuses a repeat while quota remains. Acknowledgement disclosure is closed at the source: the returned value describes only the new row, a second Student's existing report on the same target is never revealed, and `content_reports` is asserted to carry exactly nine columns with no queue, priority, assignment, or moderator surface for S5 to publish.
  One accuracy finding, recorded rather than papered over: PostgreSQL's single-argument `btrim` strips spaces only, so the constraint refuses `NULL`, empty, and spaces-only explanations while a tab- or newline-only explanation would satisfy it. The domain's `strings.TrimSpace` refuses that input before any SQL runs, so no such row is reachable through the production path. Both halves are asserted, and [data-model.md](data-model.md) now states the split instead of implying the constraint alone carries FR-029.
- [X] T068 [US4] Integration test: reported content is **not** hidden, retired, altered, reordered, or marked as a consequence of a report (FR-031, SC-011)
  Evidence: `t068_report_inertness_integration_test.go` submits through the production router against real PostgreSQL and compares whole-row snapshots of fourteen relations — Course, Revisions, Section and Lesson identities and their authored rows, `lesson_files`, media assets, Asset Versions, renditions, Accounts, Enrollment, Entitlement, and Progress — plus every rendered read model, captured before and after. Only the opaque report contexts are normalized, because a fresh nonce per render is the one value that legitimately differs; every title, state, position, availability, and material field is compared as-is. All five target kinds are reported in turn, each adding exactly one `content_reports` row and leaving the graph and the reads byte-identical. Reordering is detectable: the fixture carries two Sections and four Lessons, and the rendered Section/Lesson order and material kinds are compared alongside the authored positions. Nothing is marked — no learning table holds a report-derived column, no foreign key references `content_reports`, and no read mentions a report beyond the D-065 contexts. A second Student's reads are unchanged by the first Student's report, and the reported Course still refuses them uniformly rather than differently. On the exact-visible side, reporting stale instance A leaves A's Revision `APPROVED`, keeps B live, retires no Asset Version, and leaves S4 resolving the identical playable version. Duplicate `409`, protected `404`, and throttle `429` submissions likewise change no content and create no row.
  Non-vacuity: T068 has no assigned mutation row, so the standing clause was satisfied with three deliberate side effects injected after report creation — auto-hiding the Course (`lifecycle = 'DRAFT'`), auto-reordering the reported Lesson, and auto-retiring the reported Asset Version. Each turned this file red on the expected kinds; `report.go` was restored byte-identically after every one and the suite reconfirmed green.
- [X] T069 [US4] Assert **no** S5 route resolves, dismisses, delists, retires, suspends, or otherwise moderates a report. Resolution is S8's (FR-035)
  Evidence: `t069_report_moderation_absence_test.go` enumerates the production route table and proves exactly one mounted route mentions a report — `POST /api/v1/learn/reports` — that no route anywhere accepts a report identifier, and that the S5 group holds exactly its six accepted routes, so a moderation route that never says "report" fails too. The classification this turns on is stated rather than dodged: the API really does mount seven delist/retire/suspension routes (`admin/courses/:id/delist`, `/retire`, `/access-suspension`, `admin/taxonomy/terms/:id/retire`, and the `accounts/:id/suspension` pair), all S2 Course lifecycle and S1 Account administration that predate S5; the test asserts S5 added none of them and none is reachable with a report identifier, rather than banning ordinary administration. A production source audit adds the code half: exactly one non-test file names `content_reports` and it only inserts, no production statement writes `resolved_at` or deletes a report, the catalog/media/identity/entitlement packages reference no report symbol at all, and report creation issues no `UPDATE`/`DELETE` against any content or authority table. `t069_report_moderation_absence_integration_test.go` then fires twenty-four representative moderation requests — list, read, `PATCH`/`PUT`/`DELETE`, resolve/dismiss/close/reopen/moderate/assign/delist/retire/suspend, and `admin`/`moderation` namespaces — through the production router against real PostgreSQL: every one is refused by the router's own 404/405 contract, none leaks the report, and the report row stays byte-identical with `resolved_at` still `NULL`, alongside unchanged content and authority snapshots. Submission itself is shown to create an unresolved report and nothing else, with an acknowledgement carrying no moderation affordance.
  Non-vacuity: T069 has no assigned mutation row, so the standing clause was satisfied with two deliberate mutations — mounting `POST /learn/reports/:reportId/resolve`, and a report-triggered `UPDATE content_reports SET resolved_at`. The first turned both inventory tests red; the second turned the source audit red and, after the integration fixture was strengthened to hold two reports, the database evidence red as well. Both files were restored byte-identically and the suite reconfirmed green.

---

## Phase 7 — Polish, evidence, and convergence

- [X] T070 [P] Run the **required mutations** table below. Each must turn a test red; restore after every one. Record the mutation and its failure (SC-012)
  **All 16 rows detected.** Rows 1–15 were executed under the full procedure — detector green before the mutation, red during it, and green again after byte-identical restoration — with each file backed up outside the repository and every restoration confirmed by `sha256sum` and `cmp`. No row was credited for a compile or environment failure: all fifteen reds are assertion or constraint failures, verified by scanning each run for build and connection errors (zero matches). The working tree was compared against a pre-mutation `git diff --binary` baseline before this note was written and was byte-identical (`90e99f4f…14f3a`).
  | # | Detector | Before | Mutated | Restored |
  |---|---|---|---|---|
  | 1 | `TestEveryProtectedLearningRouteRevalidates` | PASS | FAIL — playback `evaluate calls 0 -> 0` | PASS |
  | 2 | `TestEveryProtectedLearningRouteRevalidates`, `TestProgressDelayedAfterRevocationIsRefusedBeforeMutation` | PASS | FAIL — `evaluate calls 1 -> 1`; revoked delayed write returned `204` | PASS |
  | 3 | `TestProgressConcurrentWritersPreserveMonotonicMaximum` (with the T031 set) | PASS | FAIL — converged max 40, want 85 | PASS |
  | 4 | `TestSaveProgressUsesOneStableRowAndWriteOnceCompletion` | PASS | FAIL — `prog_completion_pair` violated | PASS |
  | 5 | `TestProgressWriteCannotCarryClientCompletionClaims` | PASS | FAIL — forbidden `CompletionPercent` field | PASS |
  | 6 | `TestCompletesUsesTrustedDurationThreshold` | PASS | FAIL — nominal duration completed a Lesson | PASS |
  | 7 | `TestBoundPositionClampsUntrustedInput` | PASS | FAIL — out-of-range rejected, not clamped | PASS |
  | 8 | `TestDenialsAreByteIdentical` | PASS | FAIL — `403 NOT_AUTHORIZED`, want uniform `404` | PASS |
  | 9 | `TestDenialsAreByteIdentical` | PASS | FAIL — `X-Denial-Reason` distinguishes causes | PASS |
  | 10 | `TestProgressDelayedAfterRevocationIsRefusedBeforeMutation` | PASS | FAIL — revoked delayed write returned `204` | PASS |
  | 11 | `TestLegacyProgressGuardRefusesNonEmptyTable` | PASS | FAIL — cutover no longer aborts | PASS |
  | 12 | `TestProductionBuildHasNoEnrollmentCreationPath` | PASS | FAIL — `INSERT INTO enrollments` in `progress.go` | PASS |
  | 13 | `TestProgressUsesStableLessonIdentity` (with the T011 set) | PASS | FAIL — `enrollment_id:uuid:YES` | PASS |
  | 14 | `TestOtherWithoutExplanationIsRefusedAtTheDatabaseConstraint` | PASS | FAIL — `other` accepted with no explanation | PASS |
  | 15 | `TestDuplicateIsRefusedByThePartialUniqueIndexItself`, `TestDuplicateOpenReportFollowsVersionIndependentPolicy` | PASS | FAIL — second unresolved report accepted | PASS |
  | 16 | `TestNoCommunitySurfaceInProtectedLearningOrItsMigrations` | PASS | FAIL — `0014_protected_learning.up.sql` carries the concept `community` | PASS |
  Row 16 was blocked in the previous pass because its detector, T071, did not exist: the `community_link` mutation was applied and the whole `internal/db` and `internal/learning` integration suites passed with it in place. T071 has since been implemented, and row 16 was then run alone under the same protocol — detector green, mutation applied to `backend/internal/db/migrations/0014_protected_learning.up.sql`, detector red naming the file and the concept, migration restored byte-identically (`2da71dc1…181c08`, `cmp` clean), detector green again. Rows 1–15 were **not** rerun in that pass; their evidence above stands from the earlier run.
  Two detector observations recorded rather than worked around: row 3's `TestProgressConcurrentWritersPreserveMonotonicMaximum` is order-dependent and passed by chance on one isolated run before failing under the table's full T031+T032 set, and row 13 is caught by T010's `TestProgressUsesStableLessonIdentity` rather than by T011's enrollments-shape test.
- [X] T071 [P] Assert **D-046 absence** over the production build: no `community`, `discord`, or `telegram` match in `backend/internal/learning/`, the S5 migrations, `frontend/src/app/[locale]/learn/`, or `frontend/src/components/learning/`. No column, no payload field, no screen element, no placeholder, no "coming soon" state
  Evidence: two detectors, each where its files live. `backend/internal/learning/t071_no_community_surface_test.go` scans the production build of `backend/internal/learning/` and every S5 migration — 35 files across both roots — and `frontend/src/components/learning/t071-no-community-surface.test.ts` scans `frontend/src/app/[locale]/learn/`, `frontend/src/components/learning/`, and the learning API client's exported types. Both refuse to pass vacuously: a missing root or a root matching zero files is a failure, and the backend detector additionally asserts the S5 protected-learning migration was among the files scanned, which is what puts mutation row 16 inside its reach. Matching is semantic rather than literal — text is lowercased with every non-alphanumeric character removed before comparison, so `community_link`, `communityUrl`, `CommunityLink`, `community-link`, and `community link` are one concept, and the set covers D-046's `community`/`discord`/`telegram` verbatim plus the renames the decision would most plausibly erode into (WhatsApp, group chat, discussion group, social group/link) and the `coming soon` placeholder T071 names. String literals, SQL identifiers, object keys, JSX text, and route paths are all in scope; only delimiters are normalized away. Both detectors carry their own spelling tests, asserting fourteen variants match and that ordinary learning vocabulary — `lessonIdentity`, `courseHome`, `commit`, `communication` — does not. No allowlist was needed: all four roots are clean, with no match in production code, comments, or migrations. Row 16 of the required-mutations table is the non-vacuity proof.
- [X] T072 [P] Assert **no office-hours** element renders on ST05 or ST06 — not even an empty or "coming soon" state. Deferred to S17 (spec §Assumptions)
  Evidence: `docs/SCREENS.md` lists "upcoming office hours" as content on both screens, so the absence is a deliberate deferral rather than an oversight, and the assumption is specific about its shape — absent, not an empty or coming-soon state. Two detectors hold it. `frontend/src/components/learning/t072-no-office-hours.test.ts` walks each screen's **real render graph** from its page entry, following relative and `@/` imports through `src/` — 14 files for ST05 and 20 for ST06 — so a new office-hours component is caught the moment it is wired into either screen, and a component sitting unused elsewhere is not mistaken for something these screens render. It anchors on files each screen is known to render (`learning-views.tsx`, both dictionaries, the API client, and for ST06 the report dialog), so a broken import resolver fails loudly instead of quietly shrinking the graph. It also scans the localized copy directly, because an unused "Office hours" string is a placeholder waiting to be wired up. `backend/internal/httpapi/t072_no_office_hours_test.go` covers the half a screen cannot control: the ST05 and ST06 read models are walked property by property, including nested objects and slice elements, and the serialized JSON is checked too so a field added through an embedded struct or a custom marshaller is caught — a payload field is the upstream of an element, and an empty array is exactly how an empty state gets rendered.
  Matching is normalized rather than literal: text is lowercased with every non-alphanumeric character removed, so `office_hours`, `officeHours`, `OfficeHours`, `office-hours`, and `office hours` are one concept, alongside the renames a deferral erodes into (`upcoming session`, `live session`, `join session`, `scheduled session`, `book a slot`, `webinar`, `zoom meeting`, `Google Meet`, `meeting link`, `calendar invite`) and the `coming soon` placeholder T072 names. Bare `session`, `schedule`, `meet`, and `book` are deliberately excluded: these surfaces legitimately carry `authenticated_session`, `playback_session`, `SessionID`, and `scheduleRetry`, and a detector that fired on those would be switched off rather than fixed. Both detectors carry their own precision tests over nineteen spellings and eleven benign identifiers.
  Non-vacuity: T072 has no assigned mutation row, so the standing clause was satisfied with two deliberate additions — an `OfficeHoursPanel` rendering "Office hours — coming soon" in a component both screens render, and an `office_hours` block on the Course Home read model. Each turned its detector red naming the file or property; `learning-views.tsx` and `learning_handlers.go` were restored byte-identically and both suites reconfirmed green.
- [X] T073 [P] Verify structured logging and audit coverage for denial reasons and Progress-write failures, sufficient to reconstruct an incident without reproducing it (Principle IX)
  Evidence: the standard is reconstruction, and every protected cause answers with the same uniform `404`, so the log is the only place an expired Entitlement, a suspended Account, and a store that stopped accepting writes can be told apart. Denial coverage was already complete and is now held to that standard: the taxonomy — unauthenticated, missing capability, suspended Account, no applicable grant, expired, emergency Course suspension, retired-beyond-eligibility, dependency failure — each arrives as its own typed `deny_reason` on a `WARN` `authorization_denied` event, while the response body and every header stay byte-identical across all eight. Cardinality is pinned at exactly one authorization event plus the one separate `http_request` completion event, so middleware, handler, and repository denial paths cannot double-count. Route coverage is derived from the mounted production router rather than a list, so a new protected route cannot ship without denial observability — all six are proven. Sensitive-value exclusion is proven by planting sentinels in the report context, the explanation, the Asset Version, the Lesson id, and the session, then asserting none reaches the log; route templates are recorded rather than resolved paths, so identifiers cannot ride in through the URL. A closed field allowlist per event and a source guard against logging `err.Error()` or request fields keep it that way. "Audit" here is operational telemetry, not privileged audit persistence — S5 writes no denial row, and the contract now says so.
  Defect found and corrected: `saveProgress` claimed a database failure was "separately observable to operations", but both branches called `logDenial`, and the persistence branch used `DEPENDENCY_FAILURE` — the same reason this route already emits for the enrollment and trusted-duration lookups. A store outage was therefore indistinguishable from an entitlement problem, which is exactly the incident Principle IX requires the log to reconstruct, and the logging package's own `AuthorizationFault` comment warns against ("counting resolution faults as refusals would make a database outage look like a spike in authorization decisions"). The smallest correction adds `ProtectedWriteFailureEvent`/`protected_write_failed` at `ERROR`, carrying operation, stage, and a closed failure class and deliberately no error text, and routes only the persistence branch to it; transactional revalidation remains a denial with its typed reason, and the Student's response is unchanged. Proving it needed a fixture that actually reaches persistence — the existing doubles fail at the media boundary first, so a first attempt asserted a loop that never ran.
  Non-vacuity: T073 owns no mutation row, so the standing clause was satisfied with two narrow temporary mutations — reverting the persistence branch to `logDenial`, and adding an `entitlement_id` field to the denial event. Each turned the intended detector red; `learning_handlers.go` and `logging.go` were restored byte-identically and the suites reconfirmed green.
- [X] T074 Update `docs/BUSINESS_RULES.md`, `docs/DECISIONS.md`, or an affected contract **only if** implementation reveals a genuine conflict. Report the conflict; **do not implement around it** (Principle XI, Principle I)
  Evidence: one genuine conflict, reported rather than implemented around. **BR-116** fixed the Progress identity as `UNIQUE(enrollment_id, lesson_id)`, but the accepted implementation keys Progress on the stable Lesson identity — `CONSTRAINT prog_identity UNIQUE (enrollment_id, course_lesson_identity_id)` in `0014_protected_learning.up.sql`, with the T022 upsert's conflict target matching. The implementation is **not** the deviation: [D-060](../../docs/DECISIONS.md#d-060--s5-progress-uses-stable-lesson-identities) (product-owner instruction, 2026-08-01) had already decided the stable key, and commit `e773607` aligned `spec.md`, `plan.md`, `data-model.md`, `quickstart.md`, the contract, and `DECISIONS.md` to it — but never amended the business rule those artifacts cite. `data-model.md` was therefore attributing `UNIQUE(enrollment_id, course_lesson_identity_id)` to a BR-116 that still read `lesson_id`. BR-116 was amended in place to record D-060's identity, keeping its identifier, citing D-060 and the superseded wording explicitly rather than contradicting it silently, and adding BR-059 to its citation list because revision-survival is exactly what that rule asserts. Two residual quotations of the old wording inside S5's own artifacts — `spec.md` §C1 and `research.md` R-03 — were corrected to match; C1's substance (S5 needs the Enrollment record) is unaffected by which Lesson column the key names. No production file was touched: `gofmt` clean, `go build` clean, and `./internal/learning ./internal/httpapi ./internal/ratelimit ./internal/logging` green at `-count=1`; frontend typecheck, 159 tests, lint, and build green. `docs-guard.sh` ok across 133 Markdown files.
  Scope note — **out-of-scope drift left unmodified and reported**: four documents outside T074's named files still cite the superseded `UNIQUE(enrollment_id, lesson_id)` wording — [`docs/launch/SLICES.md`](../../docs/launch/SLICES.md) §3.4, [`docs/launch/MVP_SCOPE_RECONCILIATION.md`](../../docs/launch/MVP_SCOPE_RECONCILIATION.md), [`docs/superpowers/specs/2026-07-26-domain-data-state-design.md`](../../docs/superpowers/specs/2026-07-26-domain-data-state-design.md), and [S6's data model](../006-course-access-grant/data-model.md). These are stale *citations* of a rule now corrected at its source, not an implementation/documentation conflict, and three of the four are owned by the launch register, a platform design document, and another slice. T074 authorises `docs/BUSINESS_RULES.md`, `docs/DECISIONS.md`, and affected contracts only, so they are reported for their owners rather than edited here. Legacy-table references to `UNIQUE(user_id, lesson_id)` (migration `0001`) are correct history and were deliberately left. No other conflict was found: report throttle (`5/hour/Student`, `ProtectedLearningReportsPerHour`), duplicate granularity (`rep_no_duplicate_open`), the five `rep_target_kind` values, `btrim`-versus-`TrimSpace` explanation validation, exact-visible D-065 binding, and the D-046/S18, S17, and S8 deferrals all already matched their documentation.
- [X] T075 Retain rendered RTL/LTR viewport evidence for every S5 screen at the S2 T066 standard, referenced from the daily record (SC-010)
  **CLOSED 2026-08-06 on an independently verified retained CI artifact.** Hosted run [31059478888](https://github.com/Owlah2025/gradex/actions/runs/31059478888) — all six jobs green — on commit `976b43223aeea7f1cae5340c7477ddf273d36cfa` uploaded `gradex-s5-t075-rendered-976b43223aeea7f1cae5340c7477ddf273d36cfa`. **Artifact identity corrected 2026-08-06 (review remediation, M-2).** The byte set independently inspected at closure was artifact id `8951584075`, 1,714,849 bytes. That artifact is **no longer retrievable**: it was superseded and removed during the run's attempt history. The surviving artifact for run `31059478888` attempt 2 is the same-named `gradex-s5-t075-rendered-976b43223aeea7f1cae5340c7477ddf273d36cfa` at id `8951843227`, 1,739,891 bytes. Those are different byte sets, and this record does **not** claim `8951843227` received the byte-level audit described below — that audit was performed on `8951584075`, which cannot now be re-fetched to reproduce it. The verification narrative that follows is therefore reported as originally performed against an artifact that no longer exists, and a reviewer wanting a reproducible byte-level audit should re-run the job on the current head rather than treat the surviving artifact as the audited one. The artifact was downloaded and inspected rather than trusted: **4/4 tests `expected`** (0 skipped, 0 flaky, 0 failed) from the report's decoded payload, naming `s5-viewport-evidence.spec.ts` and the four viewport tests; **32/32 rendered cells** present with no missing, extra, or duplicated coordinate and **32 distinct SHA-256 digests**; every PNG decoded with width equal to its viewport (390/768/1280/1440) and height equal to the manifest's recorded `rendered_height` — the six taller `st07` captures are `fullPage`, so height legitimately exceeds the viewport; **manifest independently re-derived**, with the report's size and digest and all 32 screenshot digests, sizes, dimensions, and coordinates recomputed and matching, `expected_cells` and `actual_cells` both 32, run id `31059478888` attempt 2, Playwright 1.62.0; **independent sensitive-data audit passed** over all 35 files plus the report's decoded 2.87 MB payload against 13 credential-shape patterns — the single hit was `e.password=null` inside Playwright's own minified zip-crypto bundle, reviewed and dismissed as a null assignment in vendor code, and field *names* such as `report_context` appear only as assertion titles; and four cells were visually inspected — Arabic phone Report modal, English desktop Lesson Player, Arabic tablet Course Home, English phone Dashboard — each rendering the correct screen, locale, and direction with no clipping, overflow, blank state, or placeholder. The artifact carries no `FAILED` marker, no trace archive, no storage state, no `.env`, and no raw Playwright log; only the sanitized log is retained. **T076 was not begun.**
  Evidence: `frontend/e2e/s5-viewport-evidence.spec.ts` — `T075 — S5 rendered RTL/LTR viewport evidence (SC-010)`, **4 passed**, one execution per viewport each walking both locales across all four S5 screens named by [spec.md §Scope](spec.md) — ST05 (learning half), ST06, ST07, and the Report Content modal — for 4 screens × 2 directions × 4 viewports = **32 rendered cells** at phone 390×844, tablet 768×1024, laptop 1280×900, and desktop 1440×1000. T060 and T061 already owned the behavioural proofs for Course Home and the Lesson Player, so this spec does not restate them; it owns what the existing matrix did not provide — evidence that is *retained* rather than only asserted, plus viewport × direction coverage for the two screens that had none: ST05 in its active state (previously covered only at phone and desktop, and only for the expired Student) and the Report Content modal, whose only prior coverage was component unit tests, which are not rendered-browser evidence. The S2 T066 standard is met on its own terms — repeatable Playwright Chromium over the locale × viewport matrix, HTML report retained, traces failure-only — and extended with one PNG per cell under `/var/tmp/gradex-s5-e2e-evidence/t075-rendered/`, all 32 confirmed present, over 1 KB, and decodable at their declared viewport width. Evidence is retained **outside** the repository: `frontend/.gitignore` excludes `playwright-report/` and `test-results/`, so this repository keeps no rendered image evidence and committing a PNG set would invent a convention it does not have; no authentication storage state is written. Real stack throughout, no protected endpoint mocked: Next.js, a Go API compiled to an external `-o` path, a per-run isolated PostgreSQL database, a run-owned local media server, production session middleware, and real S4 playback authorization asserted at `200`. Each cell asserts direction, absence of horizontal overflow, the screen's primary Student capability present and enabled, and — on the modal — dir, title, localized copy with no raw reason enum, focus entering the dialog, a width no greater than the viewport, and `Escape` closing with focus returned to the trigger. Information hiding is asserted over served markup, URL, `localStorage`, and `sessionStorage` with delimiter-insensitive matching. Every execution fails on any `console.error` or failing `/api/v1/learn/` response. Referenced from the daily record at `docs/launch/daily/2026-08-05.md`, §"Acceptance Evidence — T075 (SC-010)", with command, scenario names, environment, matrix, and artifact path. That record is a new untracked path in this uncommitted attempt, so it is cited as a plain path rather than a Markdown link: `docs-guard.sh` requires link targets to already be in the git index, and staging it to satisfy that would violate T077's clean-tree gate.
  Retention blocker (why this task is unchecked): T075 says **Retain**, at the S2 T066 standard. That standard is recorded in [`STATUS.md`](../../docs/launch/STATUS.md) as "the retained HTML report is the configured CI artifact, while traces remain failure-only by policy" — and the repository does not implement it. `.github/workflows/ci.yml` has **no Playwright job at all** (its `frontend` job runs only `lint`, `typecheck`, `test`, `build`) and `.github/` contains **no `actions/upload-artifact` step anywhere**, so there is no configured CI artifact for a report to be. `frontend/.gitignore` excludes both `playwright-report/` and `test-results/`; `git ls-files` tracks **zero** images and no evidence directory; and S3's own T066 line records no retained path. Every candidate destination is therefore transient: the HTML report is regenerated into an ignored directory, and a `/var/tmp` path does not survive session or worktree cleanup. **No durable retained-evidence mechanism exists in this repository to retain into**, so the retention half of T075 cannot be satisfied by any amount of test work, and inventing a storage convention here would be a repository-level decision this task does not own. The rendered evidence is executed, verified, and reproducible on demand by the one command above; what is missing is a durable home for it. **Closed as infrastructure on 2026-08-05.** An `S5 T075 Rendered Evidence` job was added to `ci.yml`: PostgreSQL and Redis service containers (no MinIO — the shared E2E setup already points `S3_ENDPOINT` at the run-owned media fixture server), lockfile-pinned Chromium, the existing `global-setup`/`global-teardown` reused unchanged for service lifecycle, and **only** `frontend/e2e/s5-viewport-evidence.spec.ts` executed. Three transient-path bases became environment-overridable with their current values as defaults, so no existing suite changes behaviour: `GRADEX_E2E_TMP_DIR`, `GRADEX_T075_EVIDENCE_DIR`, `GRADEX_PLAYWRIGHT_HTML_DIR`. The HTML report, the 32 PNGs, and `manifest.json` are written into one job-scoped directory and uploaded as `gradex-s5-t075-rendered-<commit sha>`; a failing run uploads under a distinct `…-FAILED-<sha>` name so a diagnostic can never be read as retained evidence. Traces stay failure-only and are excluded from the upload, because a compressed archive cannot be audited before publication. `frontend/scripts/t075-evidence-manifest.mjs` generates the manifest, re-derives the 32-cell matrix from SC-010 and fails on any missing, extra, duplicate, invalid, or mis-sized cell as well as a missing or payload-less report, and refuses the upload on credential-shaped **values** while deliberately tolerating field *names* in assertion titles. Every guard was proved non-vacuous: a removed cell, a duplicated image, a deleted report, and a corrupted PNG each failed `verify` with exit 1; a planted `Set-Cookie` line and a planted PostgreSQL DSN each failed `audit`. A local CI-like simulation with the exact job environment gave 4 passed, 32/32 cells, integrity ok, audit passed over 34 files, and an upload directory holding exactly the report, the 32 PNGs, and the manifest. `STATUS.md`'s false claim that the report was already a configured CI artifact was corrected in the same pass. **T075 remains unchecked**: commit and push are prohibited here, so no run exists and no artifact identifier can be cited. It closes on the first successful run of that job with its artifact verified. `actions/upload-artifact` is pinned to `330a01c490aca151604b8cf639adc76d48f6c5d4` (`v5.0.0`), resolved from GitHub's tag API and matching the repository's SHA-pinning policy; every `uses:` in the workflow is now SHA-pinned.
  Non-vacuity: T075 owns no mutation-table row, so the standing clause was satisfied with two temporary defects, each backed up outside the repository and restored byte-identically (`cmp` clean; no Git restore, reset, checkout, or stash). (1) `data-report-context={target.context}` on the dialog trigger — which the component's own contract forbids ("never placed in an attribute"). The first attempt **survived**, exposing a real gap: `report_context` was on the storage list but not the rendered-output list. The detector was corrected, the clean build reconfirmed green, and the mutation then turned all four viewports red naming `rendered output must not contain report_context`; the failure names the field, never the token. A second latent gap was found and fixed the same way — the normalizer stripped non-Latin characters, which would have reduced the Arabic deferred-surface terms to empty strings and made `not.toContain("")` vacuously false, silently switching off the Arabic half of that check. (2) A 2400 px box in `CourseOutline` — red at `scrollWidth 2420 exceeds viewport 390`. A first zero-height empty box produced no overflow and correctly stayed green, so the overflow detector is confirmed non-vacuous by the box that genuinely overflows.
  Isolation: the four executions take new active rotating slots 18–21, so this matrix neither inherits nor donates Progress and spends none of the Lesson Player matrix's per-Student playback budget. The pool grew from 18 slots / 200 Students to 22 / 240 in both mirrored places — `backend/cmd/e2e-seed/rotating_students_test.go` and `frontend/src/lib/api/e2e-students.ts` — with the collision property still asserted by the unit suite over all 21 active executions per repetition. **No production file was modified**; the changes are the new spec plus test-fixture allocation. T076 was not begun: nothing measures playback timing, sets a threshold, emulates a network profile, or alters preload.
- [X] T076 **Time-to-first-frame evidence (SC-001).** Add `frontend/e2e/s5-playback-performance.spec.ts` measuring the interval from Lesson-Player navigation to the first rendered video frame (the player's first `timeupdate` past zero, or `video.getVideoPlaybackQuality().totalVideoFrames > 0` — not merely `loadedmetadata`, which precedes any visible frame). Assert **< 5 s at every one of the four supported viewports** — phone, tablet, laptop, desktop. **Do not weaken the 5-second threshold.** Determinism is a requirement of this task, not a nicety: drive a **documented, repeatable "typical connection" profile** via Chromium CDP `Network.emulateNetworkConditions` (record the exact latency, throughput, and profile name in the spec file), serve **local media fixtures** through the existing Playwright `webServer` at `127.0.0.1:3000`, and take no dependency on public-network conditions or a live CDN. Record the measured per-viewport figures and the test environment alongside the T075 rendered evidence, referenced from the daily record *(SC-001)*
  Evidence: `frontend/e2e/s5-playback-performance.spec.ts` measures navigation → first **rendered** frame at all four viewports, closed on an independently verified retained CI artifact. Hosted run [31065425952](https://github.com/Owlah2025/gradex/actions/runs/31065425952) — **all six jobs green** — on commit `ecdd43567a17983879174cb5a127e94446e8ff52` uploaded `gradex-s5-t075-rendered-ecdd43567a17983879174cb5a127e94446e8ff52` (artifact id `8953715954`, 1,937,081 bytes, expires 2026-11-04). Hosted measurements, each asserted separately and never averaged: **phone 3290 ms, tablet 3254 ms, laptop 3245 ms, desktop 3252 ms** — all strictly under the 5000 ms threshold, every one via `totalVideoFrames` with `frames=4`, so first-frame evidence is a **presented frame** in every case. `loadedmetadata`, `loadeddata`, `canplay`, `readyState`, and visibility are rejected by construction. The clock starts immediately before `page.goto` and the real Play click stays inside the interval, because SC-001 measures a Student who can start a Lesson and see video playing.
  Measured against the **built** frontend (`frontend_mode: production`) behind a run-owned loopback proxy reproducing the deployed same-origin topology — a standalone `next start` 404s the client's relative `/api/v1` calls, since production rewrites are deliberately absent and the deployed edge supplies that boundary ([D-069](../../docs/DECISIONS.md#d-069--sc-001-is-measured-against-the-built-frontend-behind-a-run-owned-same-origin-proxy)). Measured against `next dev` the same profile gave 8722–9941 ms, ~86% of it before the Play control even appeared; that measured on-demand compilation, not the product. Deterministic profile verified field-by-field: `gradex-sc001-deterministic-4g`, offline false, 150 ms latency, 500,000 B/s down, 125,000 B/s up, `cellular4g`, **cache disabled**, applied through Chromium CDP `Network.enable`/`setCacheDisabled`/`emulateNetworkConditions` — never a route delay or sleep. Origins verified: frontend `http://127.0.0.1:39999`, API **same origin** (the deployed contract), media on its own dynamic loopback port `http://127.0.0.1:35995` — never claimed to be 3000 — `all_loopback: true`, `public_dependency_count: 0`.
  Verified by inspecting the artifact bytes, not by trusting the job: the performance report's decoded payload names `s5-playback-performance.spec.ts` with **4/4 `expected`**, 0 skipped, 0 flaky, 0 failed, 0 trace references; the JSON re-derived against schema, task, requirement, threshold, commit, run id, 4/4 unique viewports with correct dimensions, numeric sub-threshold timings, corroborated signals, and exact profile — zero mismatches; the sanitized log present and confirmed **not** the raw runner log, carrying the four results with no cookie, authorization, CSRF, credential, signed URL, body, or header; and an independent audit over all 38 files plus both decoded report payloads (2.87 MB and 48 KB) against 14 credential-shape patterns, whose only hit was `e.password=null` in Playwright's own minified zip-crypto bundle — reviewed and dismissed as a null assignment in vendor code. **T075's evidence was re-verified as intact in the same artifact**: report size and digest match, 32 PNGs present with 32 distinct digests all byte-identical to the previously verified manifest. No `FAILED` marker, no trace archive, no storage state, no `.env`, no raw log. **T077 and T078 were not begun.**
- [X] T077 Local gate: after the S5 implementation commit, `git diff --quiet` and `git diff --cached --quiet` pass; NUL-delimited final porcelain status is byte-identical to the recorded pre-implementation baseline; no untracked path was introduced; no baseline path disappeared, was staged, committed, ignored, or relocated; and the authorized S5 implementation commit is present in the range. The baseline is captured dynamically with `git status --porcelain=v1 -z --untracked-files=all`, so documented pre-existing user-owned residue remains visible and uncommitted. `./scripts/s5-convergence-gate.sh` passes: the three accepted commits are present at their exact SHAs (`f81d8327` gate correction, `e7736077` D-060 reconciliation, `5cc8ede0` implementation), every later commit matches exactly one authorized subject class with its changed paths inside that class's allowlist, no commit after the implementation commit touches S5 production scope, there is no merge commit, and there is no unclassified commit. The former fixed `rev-list --count … must equal 3` was a prediction made before any CI-produced evidence existed and is superseded by [D-067](../../docs/DECISIONS.md#d-067--s5-convergence-is-gated-on-authorized-history-not-a-commit-count); membership plus path boundaries is strictly stronger than a count, which cannot tell an authorized commit from an unauthorized one of the same number. **T078 remains unchecked** pending independent review.
  Evidence: verified on the post-T076 head `d70eac698e4ba035e515a65cbfe52d142d5e6913`, before this closure commit. `./scripts/s5-convergence-gate.sh` with `S5_STATUS_BASELINE=/var/tmp/gradex-s5-attempt-2-evidence/gradex-s5-attempt-2-status-baseline.z` returned **`ok — 14 commit(s) classified, no merge commit, tree clean and equal to baseline`** (exit 0). `git diff --quiet` and `git diff --cached --quiet` both passed; the final NUL-delimited porcelain from `git status --porcelain=v1 -z --untracked-files=all` measured **0 bytes**, byte-identical to the recorded pre-implementation baseline, which is itself 0 bytes with SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` — so no untracked path was introduced and no baseline path disappeared, was staged, committed, ignored, or relocated. The three accepted commits are present at their exact SHAs and in order — `f81d8327` gate correction, `e7736077` D-060 reconciliation, `5cc8ede0` the authorized implementation commit — and every later commit matched exactly one authorized subject class with all changed paths inside that class's allowlist: `c0a8e41`/`9a371cf`/`d133ce4`/`21eeef7` GATE, `add794a` WORKFLOW, `436178d`/`976b432` CI_STABILIZATION, `732d0b7`/`ecdd435` T076_EVIDENCE, `48f7d64`/`d70eac6` CLOSURE. **No merge commit, no unclassified commit, no unauthorized path, and no commit after the implementation commit touched S5 production scope.** T077's text requires no non-vacuity proof of its own; the gate's fail-closed behaviour is nonetheless retained proof recorded under [D-067](../../docs/DECISIONS.md#d-067--s5-convergence-is-gated-on-authorized-history-not-a-commit-count), [D-068](../../docs/DECISIONS.md#d-068--s5-task-closures-are-authorized-separately-one-truthful-commit-each), [D-069](../../docs/DECISIONS.md#d-069--sc-001-is-measured-against-the-built-frontend-behind-a-run-owned-same-origin-proxy), and [D-070](../../docs/DECISIONS.md#d-070--t077-closes-on-the-verified-convergence-gate-independently-of-t078), and was re-proved for this closure class across three positive and eight negative controls.
  **Revalidated 2026-08-06 on the remediated history (Tier 3 review remediation).** The head this task
  originally closed against, `f5985c76a9c69dea7a3d2e8128ad069ae6a663fd`, is **superseded**: independent
  Tier 3 review found H-1 there, so it is no longer an eligible T078 candidate. T077's checkbox is
  unchanged — its requirement is re-established, not newly claimed. Re-verified with
  `S5_STATUS_BASELINE=/var/tmp/gradex-s5-attempt-2-evidence/gradex-s5-attempt-2-status-baseline.z`,
  baseline SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` (0 bytes):
  `./scripts/s5-convergence-gate.sh` returned **`ok — 22 commit(s) classified, no merge commit, tree
  clean and equal to baseline`** (exit 0), with `git diff --quiet` and `git diff --cached --quiet` both
  passing and the final NUL-delimited porcelain measuring 0 bytes. **No merge commit, no unclassified
  commit, no unauthorized path, and no post-implementation production-scope violation outside the single
  authorized exception** recorded in [D-071](../../docs/DECISIONS.md#d-071--playback-issuance-enforces-two-rate-ceilings-and-the-gate-opens-exactly-one-production-exception),
  which admits five exact files to `TIER3_REMEDIATION` alone and is proved class-bound rather than
  path-bound by a negative control.
  Six commits were added after `f5985c7`, classified GATE / TIER3_REMEDIATION / TIER3_EVIDENCE in pairs:
  `4b0cfb2` and `99f3ddc` gate authorization, `6d14657` and `398201f` the H-1 and M-1 implementation,
  `b1190f0` and `e7d62d8` the evidence reconciliation. The second of each pair exists because hosted run
  [31070393404](https://github.com/Owlah2025/gradex/actions/runs/31070393404) failed `Admission
  Integration` on `b1190f0`: the integration fixture's policy map was the third of three independent
  assemblies of the protected-learning policy set and had not been given the two playback endpoints, so
  `NewLearningFoundation` refused to construct. The cause was the H-1 change, not the M-1 addition.
  Hosted run [31100372500](https://github.com/Owlah2025/gradex/actions/runs/31100372500) on
  `e7d62d8678732304121d577e40abe75afc17c654` was **green on all six jobs** — Backend, Frontend,
  Migrations, Admission Integration, Guards, and S5 T075 Rendered Evidence. **Current retained continuity
  anchor for the complete T075/T076 evidence set:**
  `gradex-s5-t075-rendered-e7d62d8678732304121d577e40abe75afc17c654`, artifact id `8967303646`,
  1,939,964 bytes, not expired, expires 2026-11-04, from run `31100372500` on commit `e7d62d8`. It
  supersedes the unretrievable `8951584075` and the surviving-but-unaudited `8951843227` as the artifact a
  reviewer should fetch. **T078 remains unchecked** and requires a *fresh* independent Tier 3 verdict
  against the new frozen head: the reviewed range moved, no verdict carries forward, and this remediation
  was authored by the builder.
  **T078 was not reviewed and is not closed.** This verification is the builder's own, and builder verification is explicitly not independent review: T078 requires hosted CI green on the exact frozen head **and** a recorded independent Tier 3 reviewer verdict against one frozen commit range with every critical and high finding resolved. A local green run does not satisfy it, and a review producing no retrievable verdict is `UNAVAILABLE`, not approval. The builder never approves its own slice.
- [ ] T078 **Convergence — cannot be marked complete on local evidence.** Requires **hosted CI green on the exact head commit** (Backend, Frontend, migrations, integration, and Guards jobs) **and** a recorded **independent Tier 3 reviewer verdict** against one frozen commit range, with every critical and high finding resolved. A local green run does not satisfy this task. If the review produces no retrievable verdict, that is review `UNAVAILABLE`, **not** approval, and the slice does not close. **The builder never approves its own slice**

---

## Required mutations

Each must turn a test red. Restore after every one.

| # | Mutation | Must fail |
|---|---|---|
| 1 | Remove one `Evaluate` call from a protected handler | T038, Checkpoint B (SC-002) |
| 2 | Cache the access decision at page load and authorise the Progress write from it | T038, T042 |
| 3 | Replace `GREATEST` with the incoming value in the upsert | T031, T032 |
| 4 | Replace `COALESCE(progress.completed_at, …)` with the incoming value | T031 |
| 5 | Trust the client-reported watched percentage | T030 (SC-004) |
| 6 | Use the Lesson's nominal duration instead of the played Asset Version's trusted duration | T030 |
| 7 | Reject an out-of-range position instead of clamping it | T030 |
| 8 | Return `403` for revoked and `404` for non-existent | T037 |
| 9 | Include the typed denial reason in the response body | T037 |
| 10 | Skip revalidation on a delayed or duplicated Progress write | T033 |
| 11 | Remove the legacy-`progress` emptiness guard | T012 |
| 12 | Add an `INSERT INTO enrollments` to the Progress path | T018, T019 |
| 13 | Make `progress.enrollment_id` nullable | T010, T011 |
| 14 | Drop the `other`-requires-explanation constraint | T067 |
| 15 | Drop the report duplicate partial unique index | T067 |
| 16 | Add a `community_link` column to the S5 migration | T071 |

## Dependencies

| Phase | Blocked by |
|---|---|
| 1 — Setup | S2, S3, and S4 closing on independent verdicts |
| 2 — Foundational | Phase 1. T004 → T005 → T006 → T007 → T008 in order; T009–T012 follow the migrations |
| 3 — US1 | **Checkpoint A** — no Progress work before creation is proven impossible |
| 4 — US2 | Phase 2. Runs alongside Phase 3; T034 touches the same handlers as T023 and T025 |
| 5 — US3 | Phase 2. Independent of Phases 3–4 except that T053 shares the player shell with T027 |
| 6 — US4 | Phase 2 (T007's `content_reports` table) |
| 7 — Polish | Phases 3–6. **T078 additionally requires hosted CI and an independent reviewer** |

**Story independence.** US1, US3, and US4 are independently testable and can be verified in any order
once Phase 2 lands. US2 is a property **of** US1's and US3's endpoints rather than a separate surface,
so its tests attach to those routes — but Checkpoint B blocks slice closure regardless of story order.

## Parallel opportunities

- **Phase 1**: T002 and T003 (different languages, different trees)
- **Phase 2**: T011 and T012 after the migrations land; T018 alongside them
- **Phase 3**: T020 and T021 (different files); T027 and T028 (frontend) alongside T030
- **Phase 5**: T050, T051, T052 (distinct frontend files); T060 and T061 (distinct spec files)
- **Phase 7**: T070–T073 are mutually independent; T076 is independent of them and of T075

Backend and frontend work in Phases 3 and 5 parallelise cleanly — they touch disjoint trees and meet
only at the contract.

## Task count

**78 tasks.** Setup 3 · Foundational 16 · US1 14 · US2 10 · US3 18 · US4 8 · Polish 9.

**No task in this file may be marked complete by the planner.** Tasks are marked by the builder as
they land, and T078 is marked only against hosted CI and an independent reviewer verdict.

## MVP scope

Every task is required. **US1 plus US2 is the minimum coherent increment** — resume and completion
without enforced expiry is a paid platform giving content away, and enforcement without playback has
nothing to enforce. US3 makes them reachable by a real user; US4 (P3) is the only phase that could
slip a day without breaking the product, and FR-035 means slipping it removes a feature rather than
leaving a half-built one.
