/**
 * The landing page's two scroll targets, named once.
 *
 * The hero links to the first, the header's course entry and the personalization hand-off both
 * resolve to the second, and the stack layers carry the ids. Spelling them in each of those places
 * is how one of them ends up scrolling to nothing after a rename.
 */
export const PERSONALIZE_ANCHOR = "personalize";
export const COURSES_ANCHOR = "courses";
