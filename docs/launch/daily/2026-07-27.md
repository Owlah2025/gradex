# Launch Day Record — 2026-07-27

> State: IN_PROGRESS
> Schedule: Day 5 — API, security, and integration design
> Public go-live target: 2026-08-15
> Schedule advanced by user on: 2026-07-24
> Starting confidence: Red

## Outcome

Approve one complete MVP API/security/integration design that freezes externally visible contracts,
authentication/session behavior, backend authorization, signed delivery, provider trust
boundaries, abuse controls, reconciliation, and security verification without inventing an answer
for an open launch gate.

## Starting Evidence

- Day 4 is `CLOSED`; every July 26 `Must` and `Should` item is complete, with no carryover.
- Current branch: `feature/002-authentication-rbac`.
- Start-of-day HEAD: `1a388cb` (`docs: close July 26 domain design day`).
- `git status --short` reports only `Gradex_Financial_Model_v1.xlsx`, which is user-owned,
  untracked, intentionally untouched, and outside this design slice.
- The approved
  [platform architecture](../../superpowers/specs/2026-07-25-platform-architecture-design.md)
  was independently approved at exact commit `c9c2238` with zero critical/high findings.
- The approved
  [domain/data/state design](../../superpowers/specs/2026-07-26-domain-data-state-design.md)
  was independently approved at exact substantive commit `2e4f3e1`; every review disposition is
  resolved and no critical/high finding remains.
- M1 remains in progress and is due for final approval on July 28.
- All 20 required launch gates remain `OPEN`; the six fast-follow gates are `DEFERRED` and are not
  included in the required-gate count.
- The reconciled [Authentication/RBAC specification](../../../specs/002-auth-rbac/spec.md) is ready
  for planning but intentionally leaves token format, TTLs, cookie/client storage, refresh-token
  reuse response, rate limits, email delivery, bootstrap operation, and immediate-suspension
  mechanics to system design.
- The current Go API exposes only the video-slice `/api/v1` routes. Authentication is a
  development-only `X-Debug-User-ID` seam backed by `fake_entitlements`; the API refuses to start
  without fake mode, and no real Account, Session, refresh, CSRF, or production authorization
  implementation exists.
- No frontend or backend file changed from `1f63a59` through the start-of-day HEAD. The latest
  verified application checks therefore remain those recorded in [STATUS.md](../STATUS.md);
  application checks were not rerun for this documentation-only start.

## Must

- [x] Reconcile the closed July 26 record, approved architecture/domain designs, canonical product
  rules, Authentication/RBAC specification, launch gates, current API/auth seams, branch, HEAD,
  worktree, recent commits, and verified test state.
  **Timebox:** 30–45 minutes. **Stop condition:** every design input is tied to current repository
  evidence, and temporary seams or gated choices are labeled explicitly.
- [x] Freeze common HTTP/API contracts: versioning, media types, naming, validation, timestamps,
  integer-fils money, localization, pagination/filter/search, optimistic concurrency, idempotency,
  correlation IDs, and one safe error envelope.
  **Timebox:** 60 minutes. **Stop condition:** clients and module handlers can implement success,
  validation, conflict, retry, and non-enumerating failure behavior without inventing transport
  semantics.
- [x] Freeze Identity/session security: credential transport and storage, access/refresh format and
  configurable TTLs, hashed bearer material, rotation/family-reuse response, logout/revocation,
  concurrent sessions, CSRF/CORS, verification/reset/invitation tokens, bootstrap Admin, password
  handling, and Redis-degraded behavior.
  **Timebox:** 90 minutes. **Stop condition:** login, refresh, recovery, invitation, logout, and
  immediate suspension have exact state checks, race behavior, failure responses, and secret-safe
  handling.
- [x] Define the backend authorization policy and protected-delivery boundary across every MVP
  module.
  **Timebox:** 90 minutes. **Stop condition:** role, Account status, ownership, resource state,
  Entitlement, emergency Course suspension, Admin preview, existence masking, and signed
  asset/version delivery are deny-by-default and mapped to exact decisions and tests.
- [x] Define abuse and provider trust contracts for rate limiting, Tap callbacks and
  reconciliation, storage/CDN access, upload completion, malware scanning, transactional email,
  secrets, timeouts, retries, and safe logging.
  **Timebox:** 90 minutes. **Stop condition:** forged, replayed, duplicated, delayed, reordered,
  ambiguous, unavailable, and exhausted outcomes are explicit; open provider gates remain
  configurable and fail closed where required.
- [x] Define event/adapter schemas, compatibility rules, observability, and the security
  verification matrix.
  **Timebox:** 60 minutes. **Stop condition:** request, event, webhook, job, and provider-attempt
  correlation is traceable; schema evolution and adapter contract tests cover success and failure
  without making projections or queues authoritative.
- [ ] Write the approved design, run its self-review and documentation guard, obtain Claude's
  independent read-only review of one stable exact diff or commit, and resolve every
  critical/high finding.
  **Timebox:** 90 minutes. **Stop condition:** the reviewed artifact and verdict are recorded,
  every finding has a disposition, and July 28 can produce implementation slices without
  inventing an API or trust-boundary decision.

## Should

- [x] Include an endpoint/capability matrix covering public, Student, Instructor, Admin, webhook,
  signed-delivery, and operational surfaces with authentication, authorization, idempotency, and
  audit requirements.
- [x] Include high-risk sequence diagrams for refresh rotation/reuse, immediate suspension,
  signed playback/download delivery, verified payment completion, and reconciliation.

## Could

- [ ] Add concise non-binding JSON examples only where the error, idempotency, optimistic
  concurrency, webhook, or event contract would otherwise remain ambiguous.

No migrations or production application behavior are scheduled before the API/security design is
approved.

## Assignments

### Developer/product owner

- Approve each presented design section and any material security/operability tradeoff, especially
  browser credential handling, session/reuse policy, public error disclosure, and operational
  recovery behavior.
- Provide or approve any required launch-envelope input; unresolved legal, provider, retention,
  accounting, and operating policy remains gated rather than guessed.

### Codex/builder

- Facilitate and document the design, verify current code/document claims, keep API contracts
  aligned with module/data ownership, and define secure defaults at configurable boundaries.
- Do not implement migrations, real authentication, or provider adapters during this design
  approval loop.

### Claude/reviewer

- Review only the exact stable diff or commit after the developer/product owner has approved the
  design content; do not edit it.
- Check requirements, authorization, privacy, secret handling, CSRF/CORS, token/session races,
  idempotency, replay, concurrency, provider failure, observability, tests, and scope.

## Acceptance Evidence

Day 5 closes when:

- one developer-approved design covers common HTTP/API/error/event contracts and every MVP module
  in the approved architecture;
- authentication, refresh rotation/reuse, logout, recovery, invitation acceptance, bootstrap, and
  immediate suspension have exact race-safe behavior and configurable security parameters;
- every protected capability has a deny-by-default backend policy for role, status, ownership,
  resource state, Entitlement, and any required emergency-suspension check;
- public versus authenticated existence masking and `401`/`403`/`404`/conflict behavior are
  deliberate and consistent;
- signed playback, downloads, previews, and upload completion are scoped to the exact actor,
  operation, logical target, Asset Version, expiry, and abuse policy;
- Tap, email, storage/CDN, malware-scanning, queue, webhook, and reconciliation adapters have
  testable success, timeout, duplicate, replay, reordering, ambiguity, and exhaustion contracts;
- idempotency, optimistic concurrency, event versioning, correlation, safe logs, metrics, alerts,
  and security tests are explicit;
- `LG-003`, `LG-004`, `LG-009`, `LG-010`, `LG-014`, `LG-018`, and `LG-019` remain configurable
  boundaries unless repository evidence resolves them;
- documentation/link/reference checks and `git diff --check` pass;
- Claude reviews the exact stable artifact read-only and no critical/high finding remains;
- the developer/product owner accepts the corrected formal record;
- the worktree is clean or every remaining change is intentionally documented.

## Blockers and Risks

- All 20 required launch gates remain open. `LG-010`, `LG-014`, `LG-018`, and `LG-019` prevent
  production acceptance of webhook, scanning, email, and operating/recovery choices, but do not
  prevent provider-neutral contracts today.
- External/provider outreach remains founder-deferred to August 6, compressing the time available
  to resolve gates required by August 9 or 12.
- July 28 must approve M1 and convert the designs into dependency-ordered slices plus the
  executable delivery foundation. Slippage today directly compresses the July 29
  Authentication/RBAC implementation.
- The current API/auth seam is development-only. Its route shapes may provide implementation
  evidence, but fake identity/Entitlements and direct Redis enqueue must not become production
  authority.
- The landing FAQ still promises a fixed 150-day access term. It remains a D-026 implementation
  gap due before public release and is not changed by this design-only slice.

## Decisions

- Developer approved the complete API/security/integration design at
  [the July 27 design](../../superpowers/specs/2026-07-27-api-security-integration-design.md).
  Exact design commit: 6862db5 (docs: add API security integration design).
- /api/v1 is a private first-party JSON API. The browser uses same-origin opaque sessions with
  session-bound CSRF; bearer access/refresh tokens never enter browser JavaScript.
- Common HTTP/Problem Details, idempotency, signed cursors, module-owned authorization, rate-limit
  outage modes, protected Media delivery, external Tap ingress, transactional outbox/email, Audit,
  observability, schema evolution, and configuration/secrets boundaries are frozen in the design.
- Required provider, legal, scanning, email, retention, accounting, and operating gates remain open
  and explicitly fail closed rather than receiving invented implementation policy.

## Carryover

- None from Day 4.

## Verification In Progress

- Start-of-day reconciliation completed at `1a388cb` on
  `feature/002-authentication-rbac`.
- Git history confirms that no frontend/backend file changed from the last verified application
  baseline through the start-of-day HEAD.
- The current router, fake authenticator/Entitlement seam, configuration boundary, migration, and
  Authentication/RBAC specification were inspected directly.
- The documentation guard found no missing local link across 44 Markdown files; July 27
  changed-document BR, D, and LG references are defined, and `git diff --check` passes.
- The required-gate register contains 20 `OPEN` entries; no required gate is `RESOLVED` or
  `DEFERRED`.
- The user-owned untracked financial spreadsheet remains untouched.
- The developer-approved design commit 6862db5 passed whitespace, placeholder, internal-link,
  source/implementation-claim, and documentation-guard checks. The target contract clearly labels
  the current fake authentication and video routes as development seams.
- Independent Claude read-only review of exact commit 6862db5 and developer review of the written
  artifact remain pending.
- Claude CLI is installed locally, but three non-interactive read-only review/output invocations
  completed without a retrievable verdict. This is unavailable review evidence, not approval; the
  required independent review remains pending.

## Next Task

Obtain independent read-only review of exact commit 6862db5, resolve any critical/high finding,
and have the developer review the written design before converting it into July 28 implementation
slices.
