# Launch Day Record — 2026-07-31

> State: IN_PROGRESS
> Schedule: Day 9 — Authentication and RBAC, S1B2 (Authenticated sessions)
> Public go-live target: 2026-08-15, readiness-gated
> Schedule advanced by user on: 2026-07-26 (real calendar date at start: 2026-07-26)
> Starting confidence: Red

## Outcome

Let an Active Account sign in through the same-origin browser boundary, use and deliberately rotate
one server-authoritative independently revocable session family, recover safely from replaced or
expired session state, and log out.

This is the second of the three approved S1B slices. Recovery and the complete Student journey remain
S1B3; staff invitation, suspension administration, and the full authorization matrix remain S1C.

## Starting Evidence

- Day 8/S1B1 is `CLOSED` at reviewed implementation head `ad1b8f6`. Exact range
  `3af09bb..ad1b8f6` passed local PostgreSQL/Redis/MinIO, frontend, guard, hosted-CI, and independent
  review evidence with 0 critical, 0 high, 2 medium, and 7 low findings; verdict
  `APPROVE WITH FINDINGS`.
- Day 8 closeout commit `2185643` and the approved local-admission recovery design `d17a367` are
  pushed on `feature/002-authentication-rbac`.
- The ignored local backend environment was repaired without committing local keys. Same-origin
  anonymous bootstrap, Arabic/English policy reads, and a synthetic registration returned
  `200/200/202`.
- Start-of-day backend formatting, build, default/integration vet, `go test -race ./...`,
  documentation guard, and exposure guard pass with `GOCACHE=/tmp/gradex-go-cache`. Frontend
  typecheck, lint, and production build pass. The initial default-cache attempt was refused by the
  workspace sandbox; the supported writable-cache rerun passed.
- The existing migration-4 session schema already separates mutable session families from immutable
  credential generations and records supersession/stale-use evidence. Existing S1A password-change
  code proves atomic credential/session rotation but no public login, authenticated browser
  middleware, deliberate renewal, logout, or sign-in UI exists.
- The working tree contains only the user-owned untracked `.caveman.json` and
  `Gradex_Financial_Model_v1.xlsx`; both remain outside launch work.
- All 21 required launch gates remain `OPEN`. `LG-021` continues to block production credential
  creation, and `LG-018` continues to block production email delivery.

## Must

- [ ] **1. Freeze the S1B2 browser-session design and executable plan — 60 minutes.** Resolve the
  browser credential representation against the later same-origin system design, define route and
  cookie/CSRF contracts, run the SPIDR scope check, and write dependency-ordered tasks.
  **Stop condition:** the developer approves one bounded vertical design; the design, feature
  artifacts, status, slice register, and this record agree; documentation guard passes.
- [ ] **2. Close the S1B1 security/transport carryovers — 45 minutes.** Add typed safe internal
  admission-failure stage telemetry, reject deterministic compromised-password screening outside
  development, apply `no-store` before strict admission binding, and make readiness require the
  migration level of each enabled capability.
  **Stop condition:** staging/production fixture misuse fails configuration; public errors remain
  generic; safe logs identify only approved stages; schema-4 S1A and schema-5 admission readiness
  are both proved.
- [ ] **3. Supply the role-scoped session configuration and foundation — 60 minutes.** Replace the
  single session window with typed Student/Instructor/Admin idle and absolute expiries, general and
  highest-risk recent-authentication windows, credential/reuse timing settings, cookie keying, and
  fail-closed production validation.
  **Stop condition:** canonical role defaults and invalid combinations are tested; no session secret
  is printable; development, staging, and production compositions fail or start as designed.
- [ ] **4. Implement generic login and atomic family creation — 120 minutes.** Perform
  production-comparable dummy Argon2id work for unknown email, conceal unknown/unverified/inactive
  causes, create a family only for an Active Account, invalidate anonymous state, and issue only the
  approved host-only browser credential plus memory-only CSRF value.
  **Stop condition:** hidden login outcomes match status/body/header/cookie/timing classes; successful
  login co-commits one family/current generation/evidence and never stores plaintext credentials.
- [ ] **5. Implement authentication, deliberate rotation, reuse classification, and logout —
  120 minutes.** Resolve a trusted principal from the current generation, enforce status/epoch and
  idle/absolute expiry, rotate generation and CSRF atomically, distinguish bounded concurrent stale
  use from confirmed reuse, revoke the family on confirmed reuse, and revoke/clear on logout.
  **Stop condition:** real PostgreSQL concurrency tests prove one rotation winner, immediate
  replacement rejection, confirmed reuse revocation, mutation recheck, and post-logout denial.
- [ ] **6. Deliver bilingual sign-in and session-state screens — 60 minutes.** Add responsive
  Arabic/English login, validated internal `returnTo`, generic failure, expired/replaced/reuse,
  retry, and logout states without browser-persisted credentials.
  **Stop condition:** RTL/LTR, phone/desktop, keyboard, failure, safe return, and logout paths pass
  frontend checks and manual inspection.
- [ ] **7. Verify and independently review S1B2 — 105 minutes.** Run focused and full local gates
  with PostgreSQL, Redis, and MinIO; freeze and push the exact implementation range; verify hosted
  CI; dispatch independent Claude review from a clean disposable detached worktree; resolve every
  critical/high finding.
  **Stop condition:** every acceptance item is evidenced, the reviewer worktree is unchanged, and
  no critical/high finding remains.

## Should

- [ ] Preserve a safe validated `returnTo` across registration/verification/login where the current
  S1B1 screens can do so without pulling S1B3's integrated journey into this slice.

## Could

- [ ] Add a minimal authenticated session-debug fixture for local development only if every `Must`
  is closed and the fixture cannot enter staging or production composition.

## Acceptance Evidence

S1B2 closes only when:

1. unknown, unverified, inactive, and wrong-password login outcomes expose no hidden Account state;
2. Active login creates exactly one independently revocable family and issues no JavaScript-readable
   or browser-persisted bearer;
3. role idle/absolute and recent-authentication windows are typed, validated, and server-authoritative;
4. only the current immutable generation authenticates and concurrent rotation has one winner;
5. immediate superseded use is rejected without work, while confirmed reuse revokes the family;
6. status, epoch, idle, absolute, generation, and CSRF checks fail closed on protected requests;
7. logout revokes the current family, clears the cookie, and prevents subsequent authentication;
8. Arabic/English sign-in and session-state UI passes responsive, accessibility, lint, typecheck,
   and production-build checks;
9. full local gates, hosted CI, and independent review pass with no critical/high finding.

## Builder and Reviewer

- **Builder:** Codex under D-033.
- **Independent reviewer:** Claude against one frozen exact range from a clean disposable detached
  worktree.
- **Fallback reviewer:** `agy` through `scripts/agy-review.sh` under D-032 containment rules only if
  Claude is unavailable.

The launch plan's older command-protocol wording that mentions a Claude build assignment is
superseded by D-033 and the plan header: Claude reviews; Codex builds.

## Decisions and Blockers

- The browser credential representation must be confirmed before implementation because the older
  feature wording says access/refresh tokens while the later approved same-origin design specifies
  one opaque server-managed session cookie. The recommendation is to follow the later system design
  and explicitly reconcile the older wording rather than introduce a second browser bearer.
- `LG-021` blocks staging/production password-screen adapter evidence, but does not block the
  deterministic development and test implementation.
- `LG-018` means production email delivery remains disabled; S1B2 does not pull provider selection or
  recovery into the active slice.
- The downstream S3–S8 schedule remains unresolved. Confidence is Red with 15 calendar days
  remaining after the scheduled date.

## Closeout

Not yet closed.
