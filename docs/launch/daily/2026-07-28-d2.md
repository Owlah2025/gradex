# Launch Day Record — 2026-07-28 (D2)

> State: CLOSED
> Day: **D2** of the nineteen-day plan in
> [the execution plan §3](../AUGUST_15_EXECUTION_PLAN.md#3-nineteen-day-execution-plan)
> Public go-live target: **2026-08-15 — hard product-owner decision**
> ([D-040](../../DECISIONS.md#d-040--august-15-restored-as-the-hard-mvp-launch-date-claude-plans-antigravity-implements-claude-reviews))
> Days remaining: **18**
> Seats: Claude plans and reviews; Antigravity implements (D-040, standing)
> Starting confidence: **Amber**

> **Filename note.** The schedule calendar left closed records at `2026-07-24.md` through
> `2026-08-02.md`, so the real-calendar name `2026-07-28.md` is already taken by the closed Day 6
> record. Real-calendar records from here carry a `-dN` suffix rather than overwrite history, which
> [PLAN.md §4](../PLAN.md#4-status-and-daily-record-contract) forbids. Recorded rather than silently
> resolved.

## Outcome

**Clear the one obligation blocking S1, and buy back the schedule's largest risk with a day of
someone else's lead time.** Two of today's three `Must` items cost no engineering hours and are not
Claude's to execute; the third is the S2 plan that D3–D5 depend on.

## Starting Evidence

Reconciled from the repository at start of day, not from the previous record's claims.

- Branch `feature/002-authentication-rbac` at `7e48ef5`, synchronized with upstream. Only untracked
  file is the user-owned `.caveman.json` — untouched.
- **D1 (2026-07-27, recorded as schedule-labelled [`2026-08-02.md`](2026-08-02.md)) is `CLOSED`.**
  28 commits landed on the real day. S1C Musts 1–6 are complete; Must 7 is incomplete for one reason
  only, and it is a review obligation rather than a build one.
- **S1C is functionally complete and NOT closed.** Range `c65cd53..506e0b4` contains both
  Antigravity-authored and Claude-authored implementation, so neither party can approve it alone.
- Start-of-day backend gates pass on the current tree with `GOCACHE=/tmp/gradex-go-cache`: `gofmt`
  clean, `go build ./...`, `go vet ./...`, `go vet -tags=integration ./...`, `go test ./...`.
- Start-of-day frontend gates pass: `typecheck`, `lint`, 21 of 21 `node:test` cases.
- `scripts/docs-guard.sh` ok across 82 Markdown files; `scripts/expose-guard.sh` ok with 13 approved
  `Expose` call sites, 1 password-plaintext boundary, 2 reviewed plaintext reads. Both now read
  untracked files, per the `8b016d5` fix — the guard-fidelity carryover is closed.
- Hosted CI [run 30290157849](https://github.com/Owlah2025/gradex/actions/runs/30290157849) passed
  all five jobs on exact head `3c16122`. No application code has changed since; integration suites
  were not rerun.
- All **21** required entries in [LAUNCH_GATES.md](../../LAUNCH_GATES.md) are `OPEN`. Zero resolved.
- The [August 6 outreach pack](../outreach/2026-08-06-launch-gate-outreach.md) is still `DRAFT` on
  all four messages, with recipient placeholders unreplaced. `DRAFT` is not sent evidence.

### One conflict between sources, reconciled

[STATUS.md](../STATUS.md) says *"no S2 work begins before S1C closes."* The execution plan allocates
D2 to *"freeze S2 spec and plan."* Both stand, and the reconciliation is the word **work**:

- S2 **implementation** does not begin until S1C closes. Unchanged.
- S2 **planning** is Claude's concurrent lane under D-040 and touches no S1C contract. Freezing a
  spec commits no behaviour and cannot be mistaken for a closed slice.

If S1C's review obligation is still open at end of day, the S2 plan sits frozen and unstarted. It
does not become permission to build.

## Must

### M1 — Clear the S1C independent-review obligation (developer decision)

S1 does not close and S2 does not start until this clears. Exactly two admissible paths:

1. **Independent pass over `c65cd53..506e0b4`** by Antigravity, if its quota has returned. Dispatch
   from a disposable detached worktree at the frozen range.
2. **Recorded developer risk acceptance** naming precisely what has had no independent pass:
   `506e0b4` in full, and the `router.go` build repair inside `0b1f150`. Everything else in the range
   was independently reviewed.

Silence is neither. A third round of Claude re-reading Claude's own commits is a self-check and
closes nothing.

- **Owner:** developer chooses the path; Claude executes the dispatch or drafts the acceptance.
- **Timebox:** 45 minutes once the path is chosen.
- **Stop condition:** a recorded verdict against the exact range, or a signed risk acceptance naming
  the two unreviewed items. Anything else leaves S1C open and this becomes D3 carryover.

### M2 — Send the entire August 6 outreach pack today (developer, zero engineering hours)

Four messages, four recipients, 21 gates behind them. Seven of those gates — `LG-005`, `LG-006`,
`LG-007`, `LG-008`, `LG-010`, `LG-011`, `LG-012` — stop the cutover no matter how much code is
finished, and no manual path substitutes for any of them.

Sending on July 28 instead of August 6 converts nine days of someone else's lead time into schedule
slack, at a cost of zero engineering hours. It is the highest-value action available today and the
only one that gets strictly worse by waiting.

- **Owner:** developer/founder. Claude cannot send these.
- **Required before sending:** replace `[COUNSEL_NAME]`, `[ACCOUNTING_CONTACT_NAME]`,
  `[TAP_CONTACT_NAME]`, `[OPERATIONS_CONTACT_NAME]`.
- **Stop condition:** each row flips `DRAFT` → `SENT` with a real timestamp, only after the message
  has actually left the sender's account.

### M3 — Freeze the S2 spec and plan (Claude)

S2 is Course/Section/Lesson authoring, submission and revision workflow, Admin review, publish,
delist, retire, private-draft protection, and Admin-only pricing with audit history. 14h of
implementation effort, review tier 2, and D4–D5 are already allocated to it.

- Write `specs/003-course-authoring/` — spec, plan, contracts, data model, tasks — in the same shape
  as `specs/002-auth-rbac/`.
- No contract may overlap the S1C surface while S1C's review is open.
- Ownership and role enforcement route through the existing `identity.Authorize` decision point.
  S2 adds capabilities to the closed set; it does not add a second decision point.
- **Timebox:** 3–4 hours.
- **Stop condition:** spec, plan, and tasks committed, with every acceptance item stated as an
  observable proof rather than a restated policy.

### M4 — Put the standing brief clause into the S2 handoff (Claude)

Carried from the S1C closeout, where six instances of one defect class appeared in a single slice:
**a control that silently degrades instead of refusing.** Conditional CSRF, a defaulted recent-auth
window, an optional outbox intent, a hand-maintained matrix, a context key nobody set, an unvalidated
outbox writer.

Every Antigravity brief from S2 onward carries: *a required dependency is validated at construction
and the component refuses to build without it; no silent defaults on a security control.*

- **Timebox:** 15 minutes, folded into M3.
- **Stop condition:** the clause is in the committed S2 handoff prompt, not only in this record.

## Should

### A4 — `CARRYOVER-S1B3-VOLUNTARY-CHANGE-EVIDENCE`

Observed evidence that a voluntary password change revokes another family, rather than restated
policy. The recovery path is proven; the voluntary-change path is a different code path and is not.

**Third consecutive slice undelivered.** It has lost its place in three cut orders, which is a
scheduling failure rather than a priority judgement. It gets a named slot in the S2 tasks file today
instead of last place in a fourth cut order.

## Could

- **New low from D1:** proof 4b detects its mutation through a scan error
  (`cannot scan NULL into *string`) rather than a named assertion. It fails correctly, but for a
  reason it does not state.
- `CARRYOVER-S1C-DENIAL-VOCABULARY` (A5) — internal monitoring naming. No acceptance item depends on
  it; post-launch.

## Assignments

| Role | Holder | Scope today |
|---|---|---|
| Planner | Claude | M3, M4, and the A4 slot |
| Builder | Antigravity | Nothing until S1C clears; then S2 from the frozen plan on D3 |
| Reviewer | Antigravity for `c65cd53..506e0b4`; Claude for everything Antigravity authors | M1 |
| Developer | Owner | M1 path decision, M2 send |

Claude does not review the Claude-authored commits inside `c65cd53..506e0b4`. That is the whole
content of M1.

## Acceptance Evidence

- M1: a verdict recorded against the exact frozen range, or a risk acceptance naming `506e0b4` and
  the `0b1f150` `router.go` repair.
- M2: four `SENT` rows with real timestamps in the outreach pack.
- M3: `specs/003-course-authoring/` committed; documentation guard green; no S2 implementation
  commit exists.
- M4: the clause present in the committed handoff prompt.

## Launch Confidence

**Amber**, unchanged, and for reasons that have not moved:

- 18 real days remain and engineering is ahead of the schedule-calendar forecast that produced the
  earlier Red.
- **21 of 21 required gates are open and zero outreach has been sent.** That, not engineering
  capacity, is the binding constraint, and it stays binding until M2 happens.
- S1 is one review away from closing, and the review is blocked on an external quota rather than on
  any defect.

Confidence goes Green when gates start resolving with evidence. It goes Red if M2 slips past
July 29, because the reply-due dates behind `LG-007`/`LG-008`/`LG-010` stop fitting before the
August 10 S7 review.

## Progress

> Record state: **IN_PROGRESS**. Work began on the evening of 2026-07-27 at the developer's
> instruction rather than waiting for the morning.

### M1 — dispatched twice, cleared neither time

| Attempt | Model | Result |
|---|---|---|
| 1 | `gemini-3.1-pro-high` (assigned) | `Individual quota reached… resets in 18m59s` |
| 2 | `gemini-3.6-flash-high` (probe) | Identical — **the quota is account-wide, not per-model** |
| 3 | `gemini-3.1-pro-high`, queued past the reset | **`REJECT` — 0 critical, 2 high, 1 low.** Clean worktree, real verdict, admissible |

Both completed runs reported `touched files: 0`, both disposable worktrees were clean, and the live
repository was unchanged. Neither produced a verdict, so both are **`UNAVAILABLE`** — which
[PLAN.md §2](../PLAN.md) says is never an approval. Artifacts under
`docs/launch/review/artifacts/` (git-ignored, local only).

The useful finding is that this is a **rolling ~20-minute window**, not the multi-hour outage that
killed remediation round two. That is worth waiting out; it is the difference between S1C closing on
an independent verdict and closing on a recorded exposure.

#### The verdict, and why the wait was worth twenty minutes

`agy` on `gemini-3.1-pro-high` reviewed exact range `c65cd53..506e0b4` from a disposable detached
worktree, reported `touched files: 0`, and returned **`REJECT`** — 0 critical, 2 high, 1 low.
Artifacts: `c65cd53-506e0b4-20260727T184227Z` (git-ignored, local only).

**All three findings were reproduced independently against the code before being accepted.** None was
taken from the report.

| # | Sev | Finding | Verified |
|---|---|---|---|
| 1 | HIGH | Every mounted staff route contradicts the frozen contract in [`s1c/spec.md` §7](../../../specs/002-auth-rbac/s1c/spec.md) — `POST /staff/invitations` vs `POST /staff-invitations`, `POST /…/:id/revoke` vs `DELETE /staff-invitations/{id}`, `POST …/preview` vs `GET`, `POST …/complete` vs `POST /staff-invitation-completions`, `POST /staff/:id/suspend` vs `POST /accounts/{id}/suspension` | **Confirmed** — direct comparison of `router.go:296+` against §7 |
| 2 | HIGH | Suspension and reinstatement are gated on `CapAdminOperations`; §7 requires `SECURITY_OPERATIONS` | **Confirmed.** No privilege escalation *today* — Admin holds both — but the code and its frozen contract disagree, and the gap opens the moment roles diversify |
| 3 | LOW | `staffSuspendBodyLimit` and `staffReinstateBodyLimit` are declared and never used; both routes bypass `strictJSONMiddleware` and call `ShouldBindJSON` with no bound | **Confirmed** — the constants exist at `staff_handlers.go:23-24` with zero references |

**This is the finding that matters about the process, not the code.** Claude reviewed this same range
at Tier 3 and did not catch a single one of these. An independent model reading the same diff found
two highs in one pass. That is precisely the argument for never-self-approve, and it would have been
lost had the range closed on a risk acceptance twenty minutes earlier.

**S1C remains open. S1 does not close. S2 implementation does not start.**

### M3 and M4 — complete

[`specs/003-course-authoring/`](../../../specs/003-course-authoring/spec.md) is frozen across three
commits: `4fc4008` spec, `818b6ad` plan/research/data-model/contracts, `1be5f48` tasks and handoff.

- 45 functional requirements, each traced to its BR rule.
- The four concurrency cases the spec named are **resolved in the plan**, not left to the
  implementer — row locks with in-transaction state re-assertion, a partial unique index that makes
  duplicate submission a constraint violation, a live owner-status re-read on approval, and emergency
  suspension modelled as orthogonal to lifecycle so it never displaces a state.
- Zero new infrastructure. The `CATALOG_AND_AUTHORING` audit module value already existed.
- Two S1C lessons are encoded as requirements rather than as advice: one authorization decision point
  (FR-041), and ownership coverage derived from the live route table so an unenforced route fails a
  test instead of depending on a reviewer noticing it (FR-042).
- The standing clause (M4) is in the committed handoff brief, delivered **with all six S1C instances
  of the defect class it prevents**, so the rule arrives with its evidence.

**No S2 implementation commit exists**, which was the acceptance condition. A frozen plan is not
permission to build.

### A4 — scheduled, not deferred again

`CARRYOVER-S1B3-VOLUNTARY-CHANGE-EVIDENCE` has its own phase in
[tasks.md](../../../specs/003-course-authoring/tasks.md) (T055–T057), placed **ahead of polish** and
marked non-cuttable. Three consecutive slices lost it from a cut order; that is a scheduling failure
rather than three independent priority judgements.

### M2 — not started

The four outreach messages remain `DRAFT` with unreplaced recipient placeholders. **This is the
day's binding constraint and it is not an engineering task.**

## Closeout

> State: **CLOSED**. Reviewed range `c65cd53..edd6508`. Final head at closeout: `edd6508`.
> **S1C is closed. S1 is complete.**

### M1 — cleared on an independent verdict, after four dispatches

| # | Range | Result |
|---|---|---|
| 1 | `c65cd53..506e0b4` | `UNAVAILABLE` — account-wide quota |
| 2 | `c65cd53..506e0b4` | `UNAVAILABLE` — same, on a different model family |
| 3 | `c65cd53..506e0b4` | **`REJECT`** — 0 critical, 2 high, 1 low |
| 4 | `c65cd53..41f50aa` | **`REJECT`** — 0 critical, 1 high, 1 medium |
| 5 | `c65cd53..5f1188a` | `APPROVE` — 0/0/0/0 |
| 6 | `c65cd53..edd6508` | **`APPROVE` — 0/0/0/0**, the closing verdict |

Every run reported `touched files: 0` with a clean disposable worktree. No run was `TAINTED`.

**Round 3 findings — all reproduced against the code before being accepted, all fixed at `41f50aa`:**

- **HIGH** — every mounted staff route diverged from the frozen
  [§7 contract](../../../specs/002-auth-rbac/s1c/spec.md) on path, method, or both. Seven routes
  moved. The contract is the authority under D-040, so the routes moved rather than the specification
  being rewritten to match what shipped.
- **HIGH** — suspension and reinstatement gated on `ADMIN_OPERATIONS` where §7 requires
  `SECURITY_OPERATIONS`. Not an escalation today, since every Admin holds both; it is the gap between
  the gate that is documented and the gate that runs, and it stops being cosmetic the moment the role
  set diversifies.
- **LOW** — both suspension routes bypassed strict binding, leaving bodies unbounded and their two
  declared limit constants unreferenced.

**Round 4 finding — caught a regression the fix itself introduced:**

- **HIGH** — tightening reinstatement to require a reason left both dictionaries labelling the field
  "(optional)" and the input without `required`. A backend contract change that stops at the backend
  is half a change. Fixed at `5f1188a`.
- **MEDIUM** — S2 specification files present in the reviewed range. **Disposition: not a defect,
  and the range choice was mine.** The S2 planning commits sit between the two review points on a
  linear history, so no range excludes them. Round 5's scope dimension examined the same facts and
  recorded them as "frozen for D2 without implementation code", which is the correct reading:
  planning commits no behaviour. Recorded rather than dropped, because a finding that disappears
  between rounds should be dispositioned by name, not by silence.

### Two changes that went beyond the findings, declared rather than smuggled

1. **Reinstatement now requires a non-empty reason.** It was optional, which allowed a privileged
   account-status change to be recorded with no explanation — the same "control that degrades rather
   than refuses" class this slice was rejected for elsewhere.
2. **The preview bearer moved to the `X-Gradex-Invitation-Bearer` header.** §7 specifies `GET`; a GET
   body is not reliably carried, and a query parameter would put a one-time secret into access logs,
   referrers, and browser history. The response sets `no-store` on every exit path.

Both are recorded in the S1C spec §7 as clarifications with their dates and reasons.

### Evidence at closeout

- **Hosted CI [run 30299076346](https://github.com/Owlah2025/gradex/actions/runs/30299076346) passed
  all five jobs — Backend, Migrations, Admission Integration, Frontend, Guards — on exact head
  `edd6508`.**
- Backend `gofmt` clean, `go build ./...`, `go vet` on both tag sets, `go test -count=1 -race ./...`.
- Frontend `typecheck`, `lint`, 21 of 21 `node:test`, and a **clean** production build with `.next`
  removed first.
- Both guards pass **in a fresh shallow clone**, not only on this workstation.
- Four new tests cover the changed behaviour, and **each was verified to fail under a deliberate
  mutation**: drop strict binding from the suspend route, make the reinstatement reason optional
  again, and remove `no-store` from preview. All three mutations failed the right test and restored
  cleanly.

### The derived matrix earned its place

The authorization matrix, derived from `r.Routes()`, failed the instant the routes moved and named
every route that had lost its row. The hand-maintained version it replaced would have stayed green.
That is the S1C remediation paying for itself inside the same slice, and it is the argument for
FR-042 in the S2 plan.

### A fourth instance of the gate-fidelity class

Hosted CI failed the documentation guard on `5f1188a` while the same guard passed locally. Two links
in this record pointed into `docs/launch/review/artifacts/`, which is git-ignored — so the targets
exist here and in no fresh clone. **The guard resolves link targets against the filesystem rather
than against tracked files.**

Found by cloning the branch shallowly into a temp directory, which is what `actions/checkout` does.
That reproduction step is worth keeping: it is the only local check that tests what CI tests.

### Carryover

- **`CARRYOVER-DOCS-GUARD-IGNORED-TARGETS` (new)**: the documentation guard passes on links whose
  targets are ignored-but-present. Fix by resolving link targets against tracked files. Deliberately
  **not** fixed inside a range under review.
- `CARRYOVER-LOCAL-BUILD-CACHE` — unchanged; a frontend build is not evidence unless `.next` was
  removed first.
- `CARRYOVER-S1B3-VOLUNTARY-CHANGE-EVIDENCE` — now scheduled as S2 tasks T055–T057, ahead of polish
  and marked non-cuttable.
- `CARRYOVER-S1C-DENIAL-VOCABULARY` — post-launch.
- **New, low, inherited from D1**: proof 4b detects its mutation through a scan error rather than a
  named assertion.

### Not done

**M2 — the outreach pack is still `DRAFT` on all four messages.** No engineering work displaced it;
it is not engineering work. It remains the single largest launch risk and the only item on this
record that gets strictly worse by waiting.

### Tomorrow's first task

**Send the outreach.** Then S2 implementation may begin from the frozen plan — S1 is closed, so the
gate that blocked it is gone.
