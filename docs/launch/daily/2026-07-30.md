# Launch Day Record — 2026-07-30

> State: IN_PROGRESS
> Schedule: Day 8 — Authentication and RBAC, S1B1 (Student admission)
> Public go-live target: 2026-08-15, readiness-gated
> Schedule advanced by user on: 2026-07-26 (real calendar date at start: 2026-07-26)
> Starting confidence: Red

## Outcome

Deliver the Student admission vertical: resolve the S1A credential-creation advisories, then let a
visitor register only as a pending Student and verify exactly once through privacy-safe, rate-limited
backend and bilingual frontend surfaces.

S1B was split into S1B1–S1B3 before implementation because its original registration, session,
recovery, and UI bundle could not fit the launch plan's daily capacity without compressing failure
paths. The developer approved the split at the start of Day 8. The written design is
[S1B Student Authentication Delivery Design](../../superpowers/specs/2026-07-30-s1b-delivery-design.md).

## Starting Evidence

- Day 7/S1A is `CLOSED`. Reviewed implementation head `70b4809` passed local backend, full
  PostgreSQL/Redis/MinIO integration, frontend, and guard checks; independent review returned
  0 critical, 0 high, 5 medium, and 6 low with verdict `APPROVE WITH FINDINGS`.
- Closeout commit `f8a15f7` is pushed on `feature/002-authentication-rbac`; hosted CI
  [run 30181079456](https://github.com/Owlah2025/gradex/actions/runs/30181079456) passed all four
  jobs.
- The working tree contained only the user-owned untracked `.caveman.json` and
  `Gradex_Financial_Model_v1.xlsx`; both remain outside launch work.
- Existing production Identity code includes Account/credential/bootstrap/session schema,
  Argon2id, the controlled bootstrap command, atomic password/session rotation, the DB principal
  resolver, and deny-by-default capability middleware. No public registration, verification,
  login, recovery, or logout route exists.
- Required launch gates were 20/20 open at reconciliation. Day 8 adds `LG-021` for the external
  compromised-password screening source, making the required count 21 open and zero resolved.

## Approved S1B split

| Date | Slice | Outcome |
|---|---|---|
| Jul 30 | **S1B1 — Student admission** | Registration, verification, privacy/abuse boundaries, durable delivery intent, admission screens |
| Jul 31 | **S1B2 — Authenticated sessions** | Role-scoped windows, login, cookie/CSRF boundary, refresh rotation/reuse, logout, sign-in screens |
| Aug 1 | **S1B3 — Recovery and integration** | Password recovery, family invalidation, remaining screens, Student journey, S1B review |
| Aug 2 | **S1C — Staff lifecycle and enforcement** | Invitations, suspension, full authorization matrix, S1 integration review |
| Aug 3 | **S2 — Course authoring and review** | Starts only after S1C closes |

S3–S8 remain `TBD`; the fixed August 8–15 runway is forecast-at-risk until the downstream calendar
is reconciled. August 7 remains protected unless the developer explicitly spends it.

## Must

- [x] **1. Freeze the S1B delivery design and schedule — 45 minutes.** Record the three bounded
  verticals, their dependency order, invariants, acceptance evidence, and downstream impact before
  implementation.
  **Stop condition:** the design, plan, slice register, gate register, status, and this daily record
  agree; documentation guard passes.
- [x] **2. Resolve admission prerequisites and land the schema boundary — 90 minutes.** Bind
  bootstrap operation ID to a semantic request fingerprint; preserve original email separately;
  require a compromised-password checker; add the minimal action-secret, Identity security
  evidence, and outbox-intent structures.
  **Stop condition:** changed-fingerprint retry fails closed; plaintext remains inside the approved
  boundary; migrations prove digest-only secrets, one-way lifecycle constraints, and complete
  rollback.
- [ ] **3. Implement registration, resend, and verification commands — 150 minutes.** Create only a
  `PENDING_VERIFICATION` Student, issue no session, co-commit verification digest/evidence/outbox
  intent, conceal existing/unknown Accounts, and make verification single-use under concurrency.
  **Stop condition:** the success, duplicate, existing-email, expired, wrong-purpose, concurrent,
  and farthest-failure cases pass against real PostgreSQL.
- [ ] **4. Expose the public admission boundary — 75 minutes.** Add structural/Origin admission,
  layered identifier/network/client/global rate limits with strict safe fallback, uniform Problem
  responses, and routes for registration, verification request/resend, and token consumption.
  **Stop condition:** response status/body/header/cookie behavior does not distinguish hidden
  Account outcomes; unsafe limiter or outbox admission fails closed.
- [ ] **5. Deliver the bilingual admission screens — 60 minutes.** Add responsive Arabic/English
  registration and verification states with backend-authoritative validation and no browser
  persistence of password or bearer secret.
  **Stop condition:** RTL/LTR, responsive, error, accepted, expired/invalid, and retry guidance
  states pass frontend checks and do not claim delivery.
- [ ] **6. Verify and review S1B1 — 90 minutes.** Run focused and full local gates with PostgreSQL,
  Redis, and MinIO; freeze and push the exact range; verify hosted CI; dispatch independent Claude
  review and resolve every critical/high finding.
  **Stop condition:** all S1B1 acceptance evidence is recorded, the review worktree is clean, and
  no critical/high finding remains.

## Should

- [ ] Correct the two inaccurate test comments carried as low S1A review findings while touching
  their owning test files; do not widen production scope for comment-only cleanup.

## Could

- [ ] Add non-binding API examples only if every `Must` is closed and the examples are generated
  from or checked against the implemented request/response contracts.

## Acceptance Evidence

S1B1 closes only when:

1. public registration can create only one `PENDING_VERIFICATION` Student and no session;
2. existing-email and unknown verification requests produce no forbidden mutation or observable
   enumeration difference;
3. password policy includes a wired compromised-password checker and preserves the original email;
4. verification secrets are digest-only, expiring, purpose-bound, supersedable, and single-use
   under concurrent consumption;
5. Account/credential/secret/evidence/outbox state commits atomically or rolls back whole;
6. public admission is layered-rate-limited and fails closed when no safe limiter decision exists;
7. responsive Arabic/English admission screens pass lint, typecheck, and production build;
8. full local gates, hosted CI, and independent review pass with no critical/high finding.

## In-progress evidence

- Foundation tasks T001–T017 are complete. Student mutation routes remain unmounted.
- `go test ./...` and integration suites for `internal/db`, `internal/identity`, `internal/outbox`,
  and `internal/ratelimit` passed after migration 0005.
- The Redis integration test proved one atomic layered allow/deny decision; strict bounded local
  fallback reports a typed unavailable outcome when it cannot decide safely.
- `go vet ./...`, `scripts/expose-guard.sh .`, and `scripts/docs-guard.sh .` passed. The plaintext
  guard still reports one reviewed boundary and exactly two password reads.

## Builder and reviewer

- **Builder:** Codex, under D-033.
- **Independent reviewer:** Claude against one frozen exact range from a clean disposable detached
  worktree.
- **Fallback reviewer:** `agy` through `scripts/agy-review.sh` under D-032 containment rules if
  Claude is unavailable.

## Open decisions and blockers

- `LG-018` remains open: S1B1 builds durable notification intent and a test delivery boundary, not
  an unapproved production email provider.
- `LG-021` is new and open: production credential creation stays blocked until the
  compromised-password source and its privacy, license, availability, and monitoring evidence are
  approved.
- The S3–S8 dates and the credibility of the fixed August 8–15 runway require downstream
  reconciliation. No protected recovery day is spent silently.
- All other required launch gates remain open; confidence is Red.

## Closeout

Not yet closed.
