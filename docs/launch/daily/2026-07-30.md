# Launch Day Record — 2026-07-30

> State: CLOSED
> Schedule: Day 8 — Authentication and RBAC, S1B1 (Student admission)
> Public go-live target: 2026-08-15, readiness-gated
> Schedule advanced by user on: 2026-07-26 (real calendar date at start: 2026-07-26)
> Starting confidence: Red

## Outcome

Deliver the Student admission vertical: resolve the S1A credential-creation advisories, then let a
visitor register only as a pending Student and verify exactly once through privacy-safe, rate-limited
backend and bilingual frontend surfaces.

S1B was split into S1B1–S1B3 before implementation because its original registration, session,
recovery, and UI bundle could not fit the launch plan's daily capacity without compressing failure
paths. The developer approved the split at the start of Day 8. The written design is
[S1B Student Authentication Delivery Design](../../superpowers/specs/2026-07-30-s1b-delivery-design.md).

## Starting Evidence

- Day 7/S1A is `CLOSED`. Reviewed implementation head `70b4809` passed local backend, full
  PostgreSQL/Redis/MinIO integration, frontend, and guard checks; independent review returned
  0 critical, 0 high, 5 medium, and 6 low with verdict `APPROVE WITH FINDINGS`.
- Closeout commit `f8a15f7` is pushed on `feature/002-authentication-rbac`; hosted CI
  [run 30181079456](https://github.com/Owlah2025/gradex/actions/runs/30181079456) passed all four
  jobs.
- The working tree contained only the user-owned untracked `.caveman.json` and
  `Gradex_Financial_Model_v1.xlsx`; both remain outside launch work.
- Existing production Identity code includes Account/credential/bootstrap/session schema,
  Argon2id, the controlled bootstrap command, atomic password/session rotation, the DB principal
  resolver, and deny-by-default capability middleware. No public registration, verification,
  login, recovery, or logout route exists.
- Required launch gates were 20/20 open at reconciliation. Day 8 adds `LG-021` for the external
  compromised-password screening source, making the required count 21 open and zero resolved.

## Approved S1B split

| Date | Slice | Outcome |
|---|---|---|
| Jul 30 | **S1B1 — Student admission** | Registration, verification, privacy/abuse boundaries, durable delivery intent, admission screens |
| Jul 31 | **S1B2 — Authenticated sessions** | Role-scoped windows, login, cookie/CSRF boundary, refresh rotation/reuse, logout, sign-in screens |
| Aug 1 | **S1B3 — Recovery and integration** | Password recovery, family invalidation, remaining screens, Student journey, S1B review |
| Aug 2 | **S1C — Staff lifecycle and enforcement** | Invitations, suspension, full authorization matrix, S1 integration review |
| Aug 3 | **S2 — Course authoring and review** | Starts only after S1C closes |

S3–S8 remain `TBD`; the fixed August 8–15 runway is forecast-at-risk until the downstream calendar
is reconciled. August 7 remains protected unless the developer explicitly spends it.

## Must

- [x] **1. Freeze the S1B delivery design and schedule — 45 minutes.** Record the three bounded
  verticals, their dependency order, invariants, acceptance evidence, and downstream impact before
  implementation.
  **Stop condition:** the design, plan, slice register, gate register, status, and this daily record
  agree; documentation guard passes.
- [x] **2. Resolve admission prerequisites and land the schema boundary — 90 minutes.** Bind
  bootstrap operation ID to a semantic request fingerprint; preserve original email separately;
  require a compromised-password checker; add the minimal action-secret, Identity security
  evidence, and outbox-intent structures.
  **Stop condition:** changed-fingerprint retry fails closed; plaintext remains inside the approved
  boundary; migrations prove digest-only secrets, one-way lifecycle constraints, and complete
  rollback.
- [x] **3. Implement registration, resend, and verification commands — 150 minutes.** Create only a
  `PENDING_VERIFICATION` Student, issue no session, co-commit verification digest/evidence/outbox
  intent, conceal existing/unknown Accounts, and make verification single-use under concurrency.
  **Stop condition:** the success, duplicate, existing-email, expired, wrong-purpose, concurrent,
  and farthest-failure cases pass against real PostgreSQL.
- [x] **4. Expose the public admission boundary — 75 minutes.** Add structural/Origin admission,
  layered identifier/network/client/global rate limits with strict safe fallback, uniform Problem
  responses, and routes for registration, verification request/resend, and token consumption.
  **Stop condition:** response status/body/header/cookie behavior does not distinguish hidden
  Account outcomes; unsafe limiter or outbox admission fails closed.
- [x] **5. Deliver the bilingual admission screens — 60 minutes.** Add responsive Arabic/English
  registration and verification states with backend-authoritative validation and no browser
  persistence of password or bearer secret.
  **Stop condition:** RTL/LTR, responsive, error, accepted, expired/invalid, and retry guidance
  states pass frontend checks and do not claim delivery.
- [x] **6. Verify and review S1B1 — 90 minutes.** Run focused and full local gates with PostgreSQL,
  Redis, and MinIO; freeze and push the exact range; verify hosted CI; dispatch independent Claude
  review and resolve every critical/high finding.
  **Stop condition:** all S1B1 acceptance evidence is recorded, the review worktree is clean, and
  no critical/high finding remains.

## Should

- [ ] Correct the two inaccurate test comments carried as low S1A review findings while touching
  their owning test files; do not widen production scope for comment-only cleanup.

## Could

- [ ] Add non-binding API examples only if every `Must` is closed and the examples are generated
  from or checked against the implemented request/response contracts.

## Acceptance Evidence

S1B1 closes only when:

1. public registration can create only one `PENDING_VERIFICATION` Student and no session;
2. existing-email and unknown verification requests produce no forbidden mutation or observable
   enumeration difference;
3. password policy includes a wired compromised-password checker and preserves the original email;
4. verification secrets are digest-only, expiring, purpose-bound, supersedable, and single-use
   under concurrent consumption;
5. Account/credential/secret/evidence/outbox state commits atomically or rolls back whole;
6. public admission is layered-rate-limited and fails closed when no safe limiter decision exists;
7. responsive Arabic/English admission screens pass lint, typecheck, and production build;
8. full local gates, hosted CI, and independent review pass with no critical/high finding.

## Final evidence

- Tasks T001–T038 are complete. Registration, verification request/resend, verification
  consumption, and current-policy routes are mounted behind strict JSON, anonymous Origin/CSRF, and
  layered rate decisions.
- `make ci` passes: formatting, build, default/integration vet, `go test -race ./...`,
  documentation guard, and exposure guard are green.
- `make test-integration` passes across every backend package with PostgreSQL, Redis, and MinIO
  available. The Identity suite includes exact-once concurrent consumption, concurrent resend,
  expired/replayed/superseded bearers, hidden no-ops, farthest-write rollback, and storage canary
  sweeps. The HTTP suite proves response/timing equivalence and excludes request canaries across
  success, duplicate, resend, invalid, and limiter-denial paths.
- The Redis integration test proved one atomic layered allow/deny decision; strict bounded local
  fallback reports a typed unavailable outcome when it cannot decide safely.
- Frontend lint, typecheck, and production build pass with static `/register`, `/verify-email`, and
  `/verify-email/result` routes. Headless visual checks at 390×844 and 1440×1000 confirmed the
  Arabic-first responsive shell without clipping or horizontal overflow.
- The development-only Next rewrite was exercised against the running Go API: anonymous bootstrap,
  Arabic policy retrieval, one eligible registration, and a case-variant hidden duplicate passed
  through relative `/api` paths; both registration outcomes returned byte-identical generic 202
  bodies.
- Clean-code review removed dead admission-foundation dependencies, replaced over-wide helpers with
  request records, reused one verification timestamp, repaired stale-policy UI recovery/focus, and
  made protected-outbox nonce reservations structurally single-use. Test review kept concurrency,
  rollback, privacy, and canary assertions at observable boundaries. Documentation review corrected
  the router contract and local quickstart prerequisites.
- `scripts/docs-guard.sh` passes across 117 Markdown files. `scripts/expose-guard.sh` passes with 10
  approved `Expose` call sites, one reviewed plaintext boundary, and exactly two plaintext reads.
- The ignored local `backend/.env` still carried two retired `*_MINUTES` settings. It was not
  modified; quickstart commands were verified with those variables explicitly blanked, and the
  committed quickstart now requires alignment with the current `.env.example`.
- Frozen implementation range `3af09bb..3a9493f` passed hosted CI
  [run 30208050132](https://github.com/Owlah2025/gradex/actions/runs/30208050132): Backend,
  Frontend, Migrations, Admission Integration, and Guards were green.
- Claude Opus independently reviewed that exact range at high effort from clean disposable detached
  worktree `/tmp/gradex-s1b1-review-3a9493f`. The first verdict was `REJECT`: 0 critical, 1 high,
  6 medium, and 6 low. The blocking finding correctly showed that production could disable Student
  registration yet still configure the bootstrap Admin with the deterministic compromised-password
  fixture.
- Remediation now rejects deterministic or unapproved adapter screening in production independently
  of the Student-registration flag. It also completed the safe outbox schema, applied the configured
  screening timeout, rate-limited anonymous capability issuance, added a bounded resend timing floor
  and tighter timing-class assertion, proved wrong-purpose persistence rejection, retained signing
  keys as byte slices across HTTP composition, normalized invalid limiter identifiers into bounded
  safe classes, added localized policy cache headers, aligned frontend Unicode letter validation,
  corrected the limiter log fixture, and added production-composition/wrong-key tests. The single
  remaining low observation—attempt counters record successful consumption rather than failed
  probes—is deferred because the immutable terminal-secret schema cannot safely attach replay
  evidence without a separate attempt record; S1B2/S1B3 abuse evidence will use that separate
  boundary rather than weakening terminal immutability.
- Corrected implementation range `3af09bb..ad1b8f6` passed hosted CI
  [run 30210367125](https://github.com/Owlah2025/gradex/actions/runs/30210367125):
  Backend, Frontend, Migrations, Admission Integration, and Guards were green.
- Claude Opus independently re-reviewed the complete corrected range `3af09bb..ad1b8f6` at high
  effort from clean disposable detached worktree `/tmp/gradex-s1b1-review-ad1b8f6`. The worktree
  remained unchanged. Final result: 0 critical, 0 high, 2 medium, 7 low; verdict
  `APPROVE WITH FINDINGS`.

## Builder and reviewer

- **Builder:** Codex, under D-033.
- **Independent reviewer:** Claude against one frozen exact range from a clean disposable detached
  worktree.
- **Fallback reviewer:** `agy` through `scripts/agy-review.sh` under D-032 containment rules if
  Claude is unavailable.

## Open decisions and blockers

- `LG-018` remains open: S1B1 builds durable notification intent and a test delivery boundary, not
  an unapproved production email provider.
- `LG-021` is new and open: production credential creation stays blocked until the
  compromised-password source and its privacy, license, availability, and monitoring evidence are
  approved.
- The S3–S8 dates and the credibility of the fixed August 8–15 runway require downstream
  reconciliation. No protected recovery day is spent silently.
- All other required launch gates remain open; confidence is Red.

## Closeout

S1B1 is closed at reviewed implementation head `ad1b8f6`. All eight acceptance conditions pass:
public registration creates only a pending Student and no session; hidden outcomes are uniform;
required screening and original-email preservation are wired; verification is digest-only,
purpose-bound, expiring, supersedable, and exactly-once; source/evidence/outbox state is atomic;
public admission has layered fail-closed abuse controls; the Arabic/English frontend passes its
checks; and local, hosted, and independent-review gates are green with no critical/high finding.

Lower-severity dispositions:

- **Admission failure telemetry (medium):** S1B2 starts by adding a typed, safe internal-stage event
  for policy, screening, outbox-admission, transaction, and persistence faults. Public Problem
  collapse remains unchanged; raw causes and credential-derived values remain excluded.
- **Staging deterministic screening (medium):** no staging bootstrap is permitted before S1B2
  rejects the deterministic fixture outside development and the LG-021-approved adapter is wired.
  Production already fails configuration closed; LG-021 remains open and blocks staging evidence.
- **Action-secret attempt evidence (low):** S1B3 will add a separate immutable attempt boundary for
  known-invalid and unknown bearer probes. Terminal consumed/superseded rows remain immutable.
- **Policy-read Origin wording (low):** accepted for S1B1 as a safe cookie-bound GET with no mutation,
  protected by network/anonymous/global limits. S1C will reconcile the unqualified design wording
  while closing the full authorization matrix.
- **Late in-transaction outbox fault equivalence (low):** the pre-lookup entropy/key admission is
  uniform; persistent write failure remains a generic infrastructure fault. S9 owns explicit outbox
  health/dispatcher admission and reconciliation.
- **Strict-bind `no-store` (low):** S1B2 transport hardening will stamp `no-store` before admission
  parsing so 400/413/415/422 credential-adjacent responses inherit it automatically.
- **Display-name punctuation/digits (low):** the current Arabic/Latin-letter interpretation matches
  the approved S1B1 task, UI guidance, and frontend/backend behavior. Revisit only through a product
  rule amendment.
- **Schema readiness floor (low):** S1B2 will make readiness capability-aware so enabling admission
  requires schema 5 without unnecessarily refusing older S1A-only deployments.
- **Evidence attribution (low):** the final local gates above were rerun after remediation and are
  attributed here to reviewed head `ad1b8f6`.

Open review questions were dispositioned as follows: the reviewer correctly did not rerun external
CI/browser evidence; run IDs and local commands above are the builder evidence. Anonymous Identity
replay behavior is explicitly defined by the frozen admission contract and requires no S1B1
`Idempotency-Key`. Limiter outcome telemetry ships now; typed failure stages and platform metrics
are S1B2 carryover. Analytics is absent, so the result route emits none. S9 retains the documented
dispatcher-lifecycle obligation without changing immutable S1B1 source intent.

The next active slice is Day 9/S1B2 authenticated sessions: first close the two medium review
carryovers, then add role-scoped windows, generic login, browser session/CSRF rotation, refresh
reuse classification and family revocation, logout, and bilingual sign-in/session screens.
