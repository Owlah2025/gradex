# Launch Day Record — 2026-07-29

> State: IN_PROGRESS
> Schedule: Day 7 — Authentication and RBAC, S1A (bootstrap and Admin security core)
> Public go-live target: 2026-08-15
> Schedule advanced by user on: 2026-07-25 (real calendar date at planning time: 2026-07-25)
> Starting confidence: Red

## Outcome

Deliver the secure bootstrap Administrator through its complete six-link chain, with deny-by-default
backend authorization proven against real protected endpoints.

Student authentication moves to S1B on July 30, and staff lifecycle plus enforcement to S1C on
July 31, under the developer-approved three-way split recorded below. The S1 outcome is unchanged;
only its calendar placement is. **S1 does not close today — it closes at S1C.**

## Starting Evidence

- Day 6 is `CLOSED` at `f39257b` with no carryover. All `Must`, `Should`, and `Could` work complete.
- Branch `feature/002-authentication-rbac`, pushed, green on hosted CI.
- The delivery foundation from S0 is in place: typed configuration and secret boundary, structured
  logging with per-attempt trusted request IDs, the RFC 9457 Problem Details envelope across
  `/api/v1`, liveness and readiness probes, repository-owned migrations, and a four-job CI pipeline
  with documentation and secret-exposure guards.
- The independent review of the Day 6 range returned 0 critical, 0 high, 0 medium, 2 low; both low
  findings are fixed and accepted as reviewed-by-resolution evidence.
- Identity has no production implementation. `internal/auth` is a development-only
  `X-Debug-User-ID` seam backed by `fake_entitlements`, and the API refuses to start without it.
- Schema is at version 1 (`0001_init`), which contains no Account, credential, or Session table.
- The reconciled [Authentication/RBAC specification](../../../specs/002-auth-rbac/spec.md) is the
  requirements source; the July 26 and July 27 designs are the behavioural source.

## Must

The bootstrap chain is ordered and each link depends on the one above it. It cannot be worked out of
sequence — see [SLICES.md §5.1](../SLICES.md#51-bootstrap-chain--fixed-order-s1a).

- [x] **1. Bootstrap schema and state.** Accounts with immutable role, password credentials, an
  explicit Identity-owned constrained `PASSWORD_CHANGE_REQUIRED` marker that does not overload
  `accounts.status`, and a dedicated bootstrap singleton/completion record that does not reuse
  shared `idempotency_records` as domain uniqueness.
  **Done at `90f92ec`** — migration `0002_identity_bootstrap` adds `accounts`,
  `password_credentials`, and `bootstrap_operations`. Both M1 obligations land as database
  guarantees: `CHANGE_REQUIRED` is a `credential_state` on the credential, leaving `accounts.status`
  free to read `ACTIVE` for the same Account; and the bootstrap singleton is a `BOOLEAN PRIMARY KEY`
  constrained to `TRUE`, so a second attempt fails on the key rather than on bypassable application
  logic. `accounts.role` is immutable by trigger, `normalized_email` is unique and must be lowercase,
  a password hash must be Argon2id-encoded, and a verified timestamp cannot coexist with
  `PENDING_VERIFICATION`. Every constraint was exercised against real PostgreSQL and refused what it
  claims to refuse. The migration integration test now uses `Down()` and checks both migrations;
  CI schema verification and `MaxSchemaVersion` move to 2.
- [ ] **2. Controlled one-off bootstrap command.** Its own `cmd/` entrypoint, not an HTTP endpoint,
  ordinary worker, or schema migration.
- [ ] **3. Restricted bootstrap-session policies.** Deny-by-default typed authorization permitting
  only password change and session termination.
- [ ] **4. Mandatory password-change completion.** Normal recent-authentication rules and Argon2id.
- [ ] **5. Session rotation and restriction removal.** Atomic: neither half may land alone.
- [ ] **6. Normal Admin authorization.** Further Admins only through the invitation workflow.
- [ ] ~~**7. Registration, verification, login, refresh rotation, logout, and recovery**~~ →
  **moved to S1B, July 30** by the developer-approved split below.
- [ ] ~~**8. Staff invitations and immediate suspension enforcement**~~ → **moved to S1C, July 31**,
  together with the full authorization matrix and the S1 integration review.
- [ ] **9. July 29 gate deadline.** Confirm and record that every architecture-affecting open gate has
  a documented configurable/provider boundary, per
  [PLAN.md §7](../PLAN.md#7-gate-deadlines). [SLICES.md §7](../SLICES.md#7-gate-dependent-slices)
  already states fail-closed behaviour for `LG-014`, `LG-010`, `LG-018`, and the `LG-001`/legal pair;
  this task audits that claim against `LG-019` and the remaining architecture-affecting entries and
  links the evidence in [LAUNCH_GATES.md](../../LAUNCH_GATES.md). An unmet deadline holds confidence
  Red on its own terms.

## Scope split — developer-approved at start of day

Recorded before implementation, per [PLAN.md §2](../PLAN.md#daily-capacity): *"If the slice cannot
fit this envelope, split it before implementation. Do not compensate by removing its failure paths or
quality evidence."*

Musts 2–8 as originally written are the entire identity surface: five more bootstrap links, five
close-condition tests, and then registration, verification, login, refresh rotation with family-reuse
detection, password reset, logout and revocation, staff invitations, and suspension enforcement. That
is not a 5–6 hour implementation block, and compressing it would come out of exactly the evidence S1
exists to produce.

The developer approved this three-way split on 2026-07-29:

| Day | Slice | Contents |
|---|---|---|
| Jul 29 (today) | **S1A — bootstrap and Admin security core** | Musts 2–6, all five close-condition tests, and the Must 9 gate audit |
| Jul 30 | **S1B — Student authentication and session lifecycle** | Registration, verification, login, refresh rotation with family-reuse detection, recovery, logout/revocation, non-enumerating responses, Student auth screens |
| Jul 31 | **S1C — staff lifecycle and enforcement** | Staff invitations and initial-password setup, immediate suspension enforcement across new and existing sessions, the full role/ownership authorization matrix, and the S1 integration review |
| Aug 1 | **S2 — Course authoring and review** | Moved from Jul 30 |

**Musts 7 and 8 are therefore not today's work.** They are scheduled, not carryover, and they are not
deferred scope — every capability stays in the MVP. See [SLICES.md §5](../SLICES.md#5-s1--identity-sessions-and-rbac).

**S1 does not close today.** S1A and S1B each close on their own evidence and reviewer verdict, but
S1's complete close conditions — the full authorization matrix and enforcement proof — belong to S1C.
No S2 work begins before S1C closes.

The cost is explicit and was accepted deliberately: July 31 was a protected recovery/spillover day,
and moving S1C into it spends that buffer. **August 7 is the next protected recovery point.** Any
slip in S1A, S1B, S1C, or S2 must travel to August 7 to find slack — four slices downstream — so a
slip is materially more expensive than it was yesterday.

### Unresolved downstream conflict

Moving S2 to August 1 takes S3's day, and that displacement cascades through S4–S8, whose next free
slot is the protected August 7. This has **not** been resolved unilaterally: S3–S8 carry `TBD` days
in [SLICES.md §2](../SLICES.md#2-slice-order) rather than dates that would misrepresent the forecast
or silently spend August 7. Dependency order is unaffected; only the calendar mapping is open.
Developer decision required — recorded in the blockers table in [STATUS.md](../STATUS.md).

## Should

Deferred by developer decision — the screens ship with the backend surface they exercise, keeping
today focused on the authorization boundary that carries the security evidence.

- [ ] ~~Responsive Arabic/English authentication screens.~~ → Student auth screens to S1B (July 30);
  staff invitation and initial-password screens to S1C (July 31).

## Bootstrap close conditions

S1A does not close until all five pass. Test 3 must assert against real protected endpoints, not a
policy function in isolation, or it proves nothing about the deny path.

| # | Proves |
|---|---|
| 1 | Bootstrap cannot complete twice — same operation ID, different ID, or concurrent. |
| 2 | The plaintext bootstrap password is never persisted, logged, written into a migration, placed in process arguments, or emitted to telemetry. |
| 3 | The bootstrap Admin cannot call unrelated protected APIs before changing its password. |
| 4 | A successful password change atomically clears the requirement and rotates the Session. |
| 5 | Failure midway leaves neither a second usable Admin nor a falsely completed bootstrap marker. |

### Test 3 staging — developer-approved 2026-07-29

The complete protected Identity and staff surface does not exist until S1C, so test 3's assertion is
staged rather than weakened:

| Close | Asserts against | Recorded as |
|---|---|---|
| **S1A** (today) | Actual protected routes that already exist — the existing video and Instructor routes are acceptable **provided they exercise the real middleware and policy chain**, not a stub or an isolated policy function | **Initial end-to-end denial evidence** |
| **S1B** | Rerun and expanded across the Student authentication surface | Interim |
| **S1C** | Rerun and expanded across the complete protected Identity and staff surface | **Final full-surface proof** |

Today's result must not be recorded as the final proof. It demonstrates that the deny path works
through the real chain; it does not demonstrate coverage.

## Acceptance Evidence

Day 7 (S1A) closes when all six bootstrap links are complete, all five close-condition tests pass —
with test 3 recorded as initial end-to-end denial evidence per the staging above — the July 29
gate-boundary audit is recorded, CI is green on hosted infrastructure, and an independent read-only
review of one frozen range leaves no critical or high finding.

## Open Decisions Requiring the Developer

Both start-of-day decisions were resolved on 2026-07-29:

| # | Decision | Resolution |
|---|---|---|
| 1 | How to split S1 across July 29–31 | **Three ways.** S1A bootstrap/Admin security core today; S1B Student authentication July 30; S1C staff lifecycle, enforcement, authorization matrix, and integration review July 31. S2 moves to August 1. July 31's protected recovery day is spent; August 7 is the next. |
| 2 | Whether Arabic/English authentication screens stay in Day 7 `Should` | **Deferred.** Student auth screens to S1B; staff invitation screens to S1C. |
| 3 | How bootstrap test 3 asserts before the full surface exists | **Staged.** S1A against existing real protected routes through the real middleware/policy chain, recorded as initial denial evidence; S1C's expanded rerun is the final full-surface proof. |

One decision remains open: the S3–S8 downstream calendar displaced by S2 moving to August 1. See
the unresolved downstream conflict above.

## Closeout

Not yet closed.
