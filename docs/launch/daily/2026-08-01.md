# Launch Day Record — 2026-08-01

> State: IN_PROGRESS
> Schedule: Day 10 — Authentication and RBAC, S1B3 (Recovery and Student integration)
> Public go-live target: 2026-08-15, readiness-gated
> Schedule advanced by user on: 2026-07-27 (real calendar date at start: 2026-07-27)
> Starting confidence: Red

## Outcome

Close S1B with safe password recovery and one integrated Student authentication journey: a Student
who has lost their password recovers it without revealing whether an Account exists, cannot reuse a
reset secret, loses every prior session family on success, and must log in normally afterward.

This is the last of the three approved S1B slices. Staff invitation, suspension enforcement, and the
full authorization matrix remain S1C on August 2. **S1 does not close until S1C closes.**

## Starting Evidence

Reconciled from the repository at start of day, not from the previous record's claims.

- Day 9/S1B2 is `CLOSED` at reviewed implementation head `7d8710e` — see
  [the July 31 record](2026-07-31.md#closeout). Two consecutive frozen ranges were independently
  reviewed by `agy` and both returned `APPROVE` with 0 critical, 0 high, 0 medium, and 0 low:
  `ad1b8f6..e21d0e4` for the implementation and `e21d0e4..7d8710e` for the CI correction.
- Hosted CI [run 30251188682](https://github.com/Owlah2025/gradex/actions/runs/30251188682) passed
  all five jobs on reviewed head `7d8710e`; [run 30251733900](https://github.com/Owlah2025/gradex/actions/runs/30251733900)
  passed all five on closeout commit `3b2f7a8`.
- Branch `feature/002-authentication-rbac` is synchronized with its upstream at `3b2f7a8`. The
  working tree contains only the user-owned untracked `.caveman.json` and
  `Gradex_Financial_Model_v1.xlsx`; both remain outside launch work.
- Start-of-day gates pass on the current tree with `GOCACHE=/tmp/gradex-go-cache`: backend `gofmt`
  clean, `go build ./...`, `go vet ./...`, and `go test ./...` all pass. Frontend `typecheck`,
  `lint`, and `npm test` pass with 13 of 13 `node:test` cases green.
- Login, role-scoped windows, generation-bound CSRF, superseded-use classification, family
  revocation on confirmed reuse, logout, and bilingual sign-in and session-state screens exist. No
  password-reset request, reset-secret consumption, all-family invalidation on recovery, or recovery
  screen exists yet.
- All 21 required launch gates remain `OPEN`. `LG-021` continues to block production credential
  creation, and `LG-018` continues to block production email delivery — so recovery ships with
  delivery intent and evidence, not with a live provider.

### Carryover accepted into today

Both are recorded in the [July 31 closeout](2026-07-31.md#closeout) and are scheduled work, not
hidden blockers.

- `CARRYOVER-S1B2-RETURNTO`: the validated internal `returnTo` is implemented for sign-in only, in
  `frontend/src/app/(auth)/login/page.tsx` and `frontend/src/components/auth/login-form.tsx`, and is
  not carried across the registration or verification screens. This was S1B2's undelivered `Should`
  and belongs to today's integrated journey.
- `CARRYOVER-S1B2-CI-DRIFT`: the CI schema-version assertion is a hardcoded literal kept in step by
  comment alone and will drift again at migration `0007`; no local gate mirrors it. Today must
  dispose of this explicitly rather than carrying it a second time.

## Must

- [ ] **1. Dispose of the CI schema-drift carryover — 30 minutes.** Either derive the expected
  version from `MaxSchemaVersion` in `.github/workflows/ci.yml`, or mirror the assertion in a local
  gate script so it fails before push, or both. Doing this first means today's own migration cannot
  repeat the S1B2 failure.
  **Stop condition:** a deliberate migration-version mismatch fails the chosen gate, and the
  corrected version passes.

- [ ] **2. Implement non-enumerating reset request and single-use consumption — 120 minutes.**
  Identical response for known, unknown, unverified, and suspended Accounts. Reset secrets are stored
  as digests, expire, bind to purpose, and are consumed exactly once under a row lock.
  **Stop condition:** real PostgreSQL race tests prove exactly one consumption winner, byte-identical
  request responses across all Account states, and refusal of expired, replayed, and
  wrong-purpose secrets.

- [ ] **3. Implement atomic password replacement with all-family invalidation — 120 minutes.**
  Replace the credential, revoke *every* session family, co-commit Identity security evidence and
  outbox notification intent in one transaction, and require a normal login afterward. Recovery
  issues no session.
  **Stop condition:** a recovered Account's every pre-existing family is denied, no session cookie is
  issued by recovery, and evidence plus outbox intent co-commit or roll back together.

- [ ] **4. Deliver bilingual recovery screens — 75 minutes.** Arabic/English request and
  reset-completion screens with generic outcome copy, no Account-existence signal, and no
  browser-persisted secret.
  **Stop condition:** RTL/LTR, phone/desktop, keyboard, expired-secret, and reused-secret paths pass
  frontend checks and manual inspection.

- [ ] **5. Close `CARRYOVER-S1B2-RETURNTO` inside the integrated journey — 45 minutes.** Carry the
  validated internal `returnTo` across registration and verification, reusing the existing safe
  boundary rather than adding a second implementation.
  **Stop condition:** a hostile external `returnTo` is neutralized at every admission entry point,
  proven by the existing mutation-checked test approach.

- [ ] **6. Prove the complete Student journey end to end — 75 minutes.** Run
  `register → verify → login → rotate → reject reuse → logout → recover → login` against real
  PostgreSQL, Redis, and MinIO, and expand the staged bootstrap-Admin denial proof across S1B's
  protected Identity surface.
  **Stop condition:** the full chain passes in one run with correct Identity evidence at each step,
  and the bootstrap Admin is denied everywhere the expanded proof reaches.

- [ ] **7. Verify and independently review S1B3 and S1B as a whole — 105 minutes.** Run full local
  gates with PostgreSQL, Redis, and MinIO; freeze and push the exact range; verify hosted CI;
  dispatch the independent read-only review from a clean disposable detached worktree; resolve every
  critical/high finding.
  **Stop condition:** every acceptance item is evidenced, the reviewer worktree is unchanged, hosted
  CI is green on the exact reviewed head, and no critical/high finding remains.

## Should

- [ ] Record whether password change actually revoked another family, as observed evidence rather
  than restated policy. S1C carries this if today runs short.

## Could

- [ ] Reconcile the S1B-wide policy-denial vocabulary against API design §6.1, only if every `Must`
  is closed. S1C owns this by default.

## Acceptance Evidence

S1B3 closes only when:

1. reset request responses are byte-identical for unknown, unverified, active, and suspended
   Accounts;
2. reset secrets are stored only as digests, expire, bind to purpose, and are consumed exactly once
   under concurrency;
3. successful recovery atomically replaces the credential, invalidates every prior family, and
   co-commits Identity evidence with outbox notification intent;
4. recovery issues no session and requires a normal login afterward;
5. Arabic/English recovery screens pass responsive, accessibility, lint, typecheck, and
   production-build checks and persist no secret in the browser;
6. the validated internal `returnTo` is safe at every admission entry point, closing
   `CARRYOVER-S1B2-RETURNTO`;
7. `CARRYOVER-S1B2-CI-DRIFT` is disposed of with a gate that actually fails on a deliberate
   mismatch;
8. the complete Student journey passes end to end in one run;
9. full local gates, hosted CI, and independent review pass with no critical/high finding.

Item 9 lists three separate requirements deliberately. S1B2 proved they are not interchangeable: a
clean independent verdict was returned on a range that did not build, and the full local suite was
green while hosted CI was red.

## Builder and Reviewer

Assigned by the developer at start of day and recorded as
[D-036](../../DECISIONS.md#d-036--claude-builds-s1b3-and-agy-reviews):

- **Builder and planner:** Claude.
- **Independent reviewer:** `agy` on `gemini-3.1-pro-high` through `scripts/agy-review.sh`, under
  D-032 containment rules, against one frozen exact range from a clean disposable detached worktree.
- **Not available as reviewer for this slice:** Claude, because it authors the range.

A new decision was required because
[D-035](../../DECISIONS.md#d-035--claude-builds-s1b2-and-agy-reviews) scoped the same arrangement to
"the remainder of the active S1B2 slice", and that slice closed at `7d8710e`. Seats do not renew
implicitly. D-036 is likewise scoped to S1B3 only; S1C needs its own assignment.
[D-033](../../DECISIONS.md#d-033--codex-resumes-building-and-claude-resumes-review) stays paused
because its stated precondition — returned Codex quota — has not been met.

## Decisions and Blockers

- The S1B3 seat assignment was resolved at start of day and recorded as D-036: Claude builds, `agy`
  reviews. The seats for S1C remain unassigned by design.
- The developer chose to dispose of `CARRYOVER-S1B2-CI-DRIFT` first thing today rather than deferring
  it, because S1B3 is expected to add migration `0007` for reset secrets — the exact condition that
  broke S1B2's hosted CI.
- `LG-018` keeps production email delivery disabled, so recovery delivers notification *intent* and
  evidence rather than mail. This is a scoped limitation, not a deferral of the recovery flow.
- `LG-021` continues to block staging/production compromised-password screening evidence. Recovery
  sets a new password, so the screening adapter is on today's path for development and test only.
- The downstream S3–S8 schedule remains unresolved.

## Closeout

Not yet closed.
