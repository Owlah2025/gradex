# Launch Day Record — 2026-07-28 (D5)

> State: CLOSED
> Reviewed implementation head: `3b6d752`
> Hosted CI: [run 30370633192](https://github.com/Owlah2025/gradex/actions/runs/30370633192),
> all five jobs passed on the exact reviewed head
> Day: **D5** of the nineteen-day execution plan
> Public go-live target: **2026-08-15 — hard product-owner decision**
> Application baseline: `08b8857`, S2 complete through User Story 2
> Starting repository head: `eefaac7`, the D-042 role assignment; documentation-only after the
> application baseline
> D5 planning freeze: `3e1809d`
> Seats: Codex specifies and implements with SpecKit; Claude independently reviews and accepts under
> [D-043](../../DECISIONS.md#d-043--codex-implements-s2-d5-and-claude-independently-reviews)
> Starting confidence: **RED**

## Outcome

**Deliver S2 Phase 5 revision integrity as one bounded, independently reviewed range.**

A Published Course gains one explicit candidate revision without changing the live graph. Approval
and rejection are exact-candidate transactions; live readers stay pinned to one captured
`live_revision_id`; real PostgreSQL concurrency and mutation evidence prove the boundary.

## Starting Evidence

- S1 is complete.
- S2 foundation T001–T011 closed at `71ad368`, with hosted CI green on the code-identical
  `b8b2ccf` tree.
- S2 private authoring T012–T022 closed at `ae638c0` after two rejections.
- S2 submission and review T023–T031 closed at `08b8857`; hosted CI run `30351429941` passed all
  five jobs on that exact implementation head.
- The worktree contained only the planner's D5 specification edits and the user-owned untracked
  `.caveman.json`; the user-owned file is untouched.
- All 21 required launch gates remain `OPEN`; D5 engineering changes none of them.

## Must

### M1 — Freeze the D5 Speckit boundary (Codex) — COMPLETE at `3e1809d`

- Reconcile T001–T031 completion markers to repository evidence without rewriting their reviewed
  requirements.
- Make T032–T038 the only unchecked implementation queue.
- Encode the owner-approved candidate, read-consistency, approval, rejection, conflict, PostgreSQL,
  mutation, production-wiring, and scope conditions across the existing S2 artifacts.
- Run the Speckit cross-artifact analysis and documentation guard.

**Stop condition:** one committed planning head, no separate feature directory, no regenerated S2
specification, no ambiguity about T038's four races, and no T039+ work in the D5 queue.

### M2 — Implement T032–T038 (Codex) — COMPLETE at `3b6d752`

Run `speckit.implement` against `specs/003-course-authoring/`, restricted to T032–T038. Correct only
proven prerequisites without expanding into pricing, lifecycle/emergency controls, taxonomy
administration, search, unrelated frontend work, or unrelated refactoring.

**Evidence:** T032–T038 are marked complete; migration schema 10, candidate identity/isolation,
approval/rejection transactions, captured-pointer reads, production wiring, rollback/dependency
proofs, and the exact four races passed the full real-PostgreSQL integration suite under `-race`.
All six mutations failed their named proofs and were restored. Backend build/vet/unit-race,
frontend typecheck/lint/test/clean-build, and the production composition-root sweep are green.

The implementation landed at `00fa48c`. The review correction proving the production mutation
security boundary landed at `3b6d752`; no production behavior changed in that correction.

### M3 — Independent review (Claude) — COMPLETE

Claude receives one frozen exact commit range from a disposable detached worktree. Claude does not
implement or repair the range. Critical and high findings return to Codex; the frozen specification
changes only if a genuine planning contradiction is found.

**Stop condition:** Claude returns `ACCEPT` or `ACCEPT WITH LOW-RISK FOLLOW-UP`, with no unresolved
critical/high finding, and hosted CI is green on the exact reviewed head.

#### Review round 1 — REJECTED

Claude Opus independently reviewed frozen range `0811ca5..00fa48c` from a clean disposable detached
worktree and returned 0 critical, 1 high, 2 medium, and 6 low findings. The worktree remained
unchanged.

The high finding was valid: the production composition-root sweep enumerated every D5 route but did
not behaviorally prove that every D5 mutation rejects missing/foreign Origin and missing/invalid
CSRF before authentication and domain work. Removing both catalogue mutation-security middleware
registrations left the test green.

Codex added one table-driven production-router regression matrix covering every exact non-GET
candidate/revision/review route in the D5 inventory. It asserts the status and Problem Details code
for all four `403` cases and the valid-browser, anonymous `401` case. Removing both affected
middleware registrations now makes the matrix fail, the production code was restored, and the full
backend build/vet/unit-race/integration-race suite is green. The corrected range must receive a fresh
independent Claude review; round 1 does not approve it.

#### Review round 2 — APPROVE WITH FINDINGS

Claude Opus independently reviewed the complete corrected range `0811ca5..3b6d752` from a fresh
clean disposable detached worktree. It returned 0 critical, 0 high, 3 medium, and 8 low findings,
with verdict `APPROVE WITH FINDINGS`; the worktree remained unchanged.

The three non-blocking medium follow-ups are preserved:

1. the pre-D5 `POST /api/v1/courses` route has the production mutation-security middleware but is
   outside the exact D5 route inventory and lacks the new behavioral Origin/CSRF matrix;
2. `GetOwnedCourse` correctly captures the live pointer once, but duplicates that logic rather than
   calling the separately tested `GetLiveCourseGraph` seam;
3. the shared Problem Details design documents `errors`, while the frozen S2 submission contract
   documents `violations`; the implementation carries both fields for their distinct validation
   classes.

The eight low findings cover test-hook placement, one direct `pgx.ErrNoRows` comparison, future
retired-taxonomy remediation, an unnecessary test helper type assertion, incomplete `:revisionId`
substitution in an authorization-test helper, an unreachable candidate-clone inconsistency path, a
contract `type`-URI typo, and the then-stale STATUS reconciliation line. None is critical or high;
none silently expands D5.

Hosted CI run
[30370633192](https://github.com/Owlah2025/gradex/actions/runs/30370633192) then passed Guards,
Backend, Frontend, Migrations, and Admission Integration on exact reviewed SHA
`3b6d7520537cb9f9c2bf63da5b24ef59c83a5710`. D5 therefore satisfies its closure contract.

## Proven-Behavior Corrections Admitted to D5

Repository inspection after D4 closure found five prerequisites that T032–T038 cannot safely inherit:

1. `SubmitCourse` contains an enabled `23505 → success` mutation instead of the frozen `409`
   behavior.
2. candidate submission and review paths can treat Asset Version validation and outbox evidence as
   optional or ignore writer errors.
3. authoring mutations infer the editable revision rather than receiving an explicit candidate
   identity.
4. the current schema gives revision-owned Section/Lesson rows no stable logical identity; cloning
   them naïvely would violate BR-059 progress preservation and BR-019 Section scope.
5. the production catalogue router receives but does not apply the required session-mutation
   foundation, leaving authoring/review mutations outside the established origin/CSRF boundary.

They are recorded as D5-C01 through D5-C05 in T032–T035 as minimal D5 corrections. Their discovery
does not authorize any other Phase 1–4 rewrite.

## M1 Planning Verification

- `.specify/feature.json` still selects `specs/003-course-authoring`; the prerequisite check returned
  that existing feature with `research.md`, `data-model.md`, contracts, `quickstart.md`, and
  `tasks.md`. No Phase 5 feature directory was created and S2 was not regenerated.
- A read-only Speckit consistency audit first rejected the draft for unstable Section/Lesson
  identity, incomplete BR traceability, implicit response semantics, insufficient clone/membership
  verification, stale handoff metadata, and the cross-Course live-pointer gap. Each finding is
  resolved in the frozen artifacts; the bounded second pass found no remaining medium-or-higher
  inconsistency.
- FR-046–FR-055 and SC-010–SC-014 map to T032–T038 with named real-PostgreSQL, route, response,
  mutation, or composition-root evidence.
- `git diff --check`, `scripts/docs-guard.sh`, and `scripts/expose-guard.sh` pass. This is a planning
  gate only; no application-code or implementation-test claim is made.

## Acceptance Evidence

- Existing S2 `spec.md`, `plan.md`, `data-model.md`, contracts, `quickstart.md`, checklist, and
  `tasks.md` agree on the D5 boundary.
- T001–T031 are marked complete with commit and closeout evidence.
- T032–T038 are complete; T039–T064 remain unchecked.
- Candidate uniqueness has a schema-10 migration and an independent invariant mutation.
- Stable Section/Lesson identity survives candidate cloning and video replacement; delete/recreate
  gets a new identity, and cloning creates no externally owned row.
- The exact four races are: concurrent first edits, concurrent approvals, approval versus a
  submitted-candidate Instructor mutation, and approval versus rejection.
- Every one of the six mutations records what it proves and what it does not prove.
- Production wiring is checked through `buildProductionFoundations` and the real router, including
  mandatory session mutation security on every explicit candidate/revision/review D5 mutation.
- Local gates and hosted CI pass on exact reviewed head `3b6d752`.
- Claude independently accepted frozen range `0811ca5..3b6d752` with no critical or high finding.

## Scope Stop

D5 stops after T038. T039–T064 remain unchecked. No pricing, lifecycle/emergency control, taxonomy
administration, search, unrelated frontend work, or unrelated refactoring enters the range.

## Launch Confidence

**RED**, unchanged. D5 can improve technical delivery evidence but cannot resolve the missing
counsel, accountant, Tap response, or any of the 21 open launch gates.
