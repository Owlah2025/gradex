# Local secrets / environment config — never commit, use .env.example instead
.env
.env.*
!.env.example

# Go build/test outputs
*.exe
*.test
*.out

# Editors and operating-system metadata
.DS_Store
Thumbs.db
*.tmp
*.swp
.idea/
.vscode/

# Personal Claude Code permissions/config — machine-local, not shared
.claude/settings.local.json

# Local documentation review bundles — generated artifacts, not source
/gradex-spec-review.zip
/docs/gradex-spec-review.zip

# Independent-review run artifacts from scripts/agy-review.sh — the verdict and finding
# dispositions are transcribed into the daily record, which is the committed evidence
/docs/launch/review/artifacts/
